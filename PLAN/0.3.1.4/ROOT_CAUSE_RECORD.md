# DarkGrey_RPG 0.3.1.4 — Root Cause Record

> 本文件是施工中的可追溯根因账本。`ROOT_CAUSE_CONFIRMED` 不等于 `IMPLEMENTED`，`IMPLEMENTED` 不等于真实 Release EXE 已验收。

| ID | 初始状态 | 已确认根因 / 风险 | 修复状态 | 自动化证据 | Release EXE 证据 |
|---|---|---|---|---|---|
| 0313-01 | `SOURCE_CONFIRMED / NEEDS_LIVE_REPRO` | canonical value、UI draft 和 commit boundary 混在 TwoWay setter；逐字 mutation / refresh 使临时无效文本和清空状态不可表达。 | `IMPLEMENTED / AGENT_VERIFIED_STAGE1` | Core `355/355`；WPF `390/390`；20 次 draft 不逐字增 revision、invalid draft 保留、Ctrl+S flush、单步 Undo/Redo 均有覆盖。 | Stage 1 最终 staging Release 候选实测 41 个中文字符、焦点内 Ctrl+S、整段 Undo/Redo、save/close/reopen 均通过；尚非 `dist` 权威包，尚非 `USER_ACCEPTED`。 |
| 0313-02 | `SOURCE_CONFIRMED / NEEDS_LIVE_REPRO` | 节点删除入口使用阻塞确认框，且没有与 Undo 事务统一。 | `IMPLEMENTED / LIVE_GATE_DEFERRED_BY_USER` | 右键与 Delete 统一直达 Core referenced-removal 事务；节点 + incident wires 单步 Undo、固定节点拒绝、placement 保留资源本体均已覆盖；Stage 4 Core `364/364`、WPF `405/405`。 | 用户要求暂时搁置实机验收；无弹窗、一次 Undo 与资源级删除确认的真实窗口 Gate 均待重新授权，不记 `AGENT_VERIFIED`。 |
| 0313-03 | `SOURCE_CONFIRMED / NEEDS_LIVE_REPRO` | hover candidate 与实际命中/离开/取消清理不共享唯一状态机。 | `IMPLEMENTED / LIVE_GATE_DEFERRED_BY_USER` | Core `363/363`；WPF `402/402`；hover/drop 共用 candidate、每 move 清旧 glow、valid/invalid/null target 均有覆盖。 | 用户暂停实机验收前已采集 valid→outside 连续帧；完整 S6 需重新授权后从干净 fixture 重跑，当前不记 `AGENT_VERIFIED`。 |
| 0313-04 | `SOURCE_RISK_IDENTIFIED / NEEDS_LIVE_REPRO` | pending click、普通 add、Ctrl bundle move 与 occupied-single reconnect 没有四手势明确分流；几何可在未达阈值时出现。 | `IMPLEMENTED / LIVE_GATE_DEFERRED_BY_USER` | 四手势 enum、pending threshold、正式 Path reconnect、ordinary/Ctrl、Escape/invalid/blank 与 cardinality matrix 自动化通过；wire-focused `50/50`。 | 用户暂停前完成 20 次 light click 与 Flow single 五帧局部证据；Logic single、Flow/Logic multi 仍待重新授权，不记 `AGENT_VERIFIED`。 |
| 0313-05 | `SOURCE_CONFIRMED / NEEDS_LIVE_REPRO` | 通用 Inspector 排列未形成 DisplayName 主值、ID 辅助值、owned/referenced 解释的层级。 | `IMPLEMENTED / LIVE_GATE_DEFERRED_BY_USER` | Actor/Actor Group/Item/Item Group 的作者术语、11px secondary label、14px primary value、ID/tags、纵向滚动及主要区移除“拥有/引用状态”已有 WPF 断言；Stage 4 WPF `405/405`。 | Light/Dark 下真实可读性、滚动和 selected resource 对应关系待用户重新授权，不记 `AGENT_VERIFIED`。 |
| 0313-06 | `SOURCE_CONFIRMED / NEEDS_LIVE_REPRO` | 节点坐标仅存在于进程内 host；缺少独立于 canonical Runtime JSON 的 Studio layout sidecar。 | `IMPLEMENTED / AGENT_VERIFIED_STAGE2` | Core `363/363`；WPF `394/394`；sidecar roundtrip/kind isolation/finite/stale cleanup/stable ID/canonical byte identity/package exclusion/restart fixture 均覆盖。 | Stage 2 staging Release 候选：真实拖动 1 Story + 2 Session + 2 Task，Save All、关闭、Dark/Light 重启后五图坐标漂移均为 0；viewport 隔离、rename stable connection、sidecar hash 不变均通过；尚非 `dist` 权威包，尚非 `USER_ACCEPTED`。 |
| 0313-07 | `ROOT_CAUSE_CONFIRMED` | Objective selection 的 canonical mutation + option list 重建 + WPF TwoWay selection 回写形成 UI thread 无限重入并 `STACK_OVERFLOW`；旧 Objective 还可能因无关 legacy validation issue 拒绝合法 target mutation，触发 snapback 后再次进入 setter。 | `IMPLEMENTED / AGENT_VERIFIED_STAGE1` | Objective type/target 改为原子事务；projection guard、稳定 ID 选择与 targeted refresh 已覆盖；100 次 target/type 自动化与全量回归通过。 | Stage 1 最终 staging Release 候选：Objective target `100` 次、type `100` 次，0 crash、最终右侧/内联一致、max 分别 `108.91 ms` / `103.11 ms`；尚非 `dist` 权威包，尚非 `USER_ACCEPTED`。 |
| 0313-08 | `SOURCE_RISK_IDENTIFIED / NEEDS_LIVE_REPRO` | Choice option 内容行和输出端口由分离投影/布局链生成，缺少同一稳定 ID/行模型与几何契约。 | `IMPLEMENTED / LIVE_GATE_DEFERRED_BY_USER` | Choice 专用 ordered row projection 由稳定 `option_id` / `flow_port_id` 建立；1/2/5/10、长文本固定 Flow anchor X、rename/reorder 保持 selection/position/connections、非 Choice 原投影均有自动化覆盖；Stage 4 WPF `405/405`。 | 真实 add/delete/connect/save/reopen、Light/Dark、100%/125%/150% DPI 及紧凑 Logic diamond 可读性待重新授权，不记 `AGENT_VERIFIED`。 |

后续每个 Work Package 完成时更新对应行，并把“源码已改”“测试已过”“真实 EXE 已操作”分栏记录。
