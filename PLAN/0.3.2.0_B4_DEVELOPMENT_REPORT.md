# DarkGrey RPG 0.3.2.0_B4 Development Report

Status: `AGENT_REAL_MACHINE_VERIFIED / USER_ACCEPTED=NO / STUDIO_FROZEN=NO`.

Construction and agent real-machine acceptance completed on 2026-09-08. Historical phase/attempt entries below retain their original evidence boundaries; the Delivery and Final closure sections describe the current result.

## Baseline and scope

- Construction specification: `DarkGrey_RPG_0.3.2.0_B4_Construction_PLAN_FINAL.md`.
- Started from verified B3 HEAD `805fed51913c637fe3a8c730f7310b2f5d114b2d`.
- Branch: `codex/0.3.2.0_B4`; no implementation commit, push, tag or release.
- Existing historical PLAN deletions, untracked material, `.codex/config.toml` and `AGENTS.md` changes are outside this implementation and retained.
- Phase order follows the specification. Automated checks do not establish real Studio/Minecraft acceptance or user acceptance.

## Phase 0

- Java baseline: offline `build` plus registered JavaExec probes except the external-input `dgrsPackageRuntimeProbe`; BUILD SUCCESSFUL, 69 tasks (47 executed).
- Studio baseline: Core 378/378 and WPF 443/443 on E-drive .NET 10 SDK.
- Initial Studio invocation selected system .NET 8 and failed NETSDK1045; corrected invocation used `E:/Java/dotnet-sdk-10/dotnet.exe`.
- Evidence: `.tooling/0.3.2.0_B4/baseline/java.log`, `studio-net10.log` and TRX files.
- Native Worker readiness preflight passed. Initial ownership: Main owns lifecycle architecture, baseline and integration; Worker owns bounded read-only Legacy/ID dependency audit.

## Phase 1 (automated verification passed)

- Added a repeatable bartender fixture to the existing production-coordinator probe.
- Before repair, Run1 completed and retained terminal Task; Run2 failed with `Repeat Run 2 must start at zero with an ACTIVE Task`.
- Evidence: `.tooling/0.3.2.0_B4/repeat-before.log`.
- Main owns explicit Start disposition and validated Story/Session lifecycle boundary. Worker owns exact player/story Task purge, including raw persistence and index isolation.
- Integrated Story/Session/Task persistence and B3 fingerprint/generation probes passed; the coordinator fixture initially used a stale test transport ID, corrected by retaining the newly returned Session frame.
- `.tooling/0.3.2.0_B4/phase1-repeat-after.log`: Run1 settlement/terminal retention, Run2 zero/settlement, Run3 zero, prior Session/continuation removal, invalid Start preservation passed.
- `.tooling/0.3.2.0_B4/phase1-purge-after.log`: active/settled/cancelled/error Task removal in both bound and raw NBT paths, subscription cleanup, other-player and other-Story isolation passed.
- Task.start retains its original idempotent behavior. Story start disposition is carried explicitly into the production coordinator; only a validated REPEATABLE_RESTART purges previous Task instances before routing new children.
- Worker Task package adopted after bounded diff review and integrated probe; Main added cancelled/error coverage. Phase 2 eligibility remains Main-owned because it shares the lifecycle/candidate contract.

## Remaining phases

Implementation, automated regressions, authoritative local delivery and PLAN 54-60 agent real-machine acceptance are complete. Explicit user acceptance and architecture freeze remain pending; no subsequent version or release is authorized by this result.

## Phase 2 (automated verification passed)

- Start eligibility excludes ACTIVE and ONCE terminal Story before matching Actor/Region triggers; direct Entry/Actor/Region reentry carries an ineligible disposition and never routes children. Logic Starts return no dispatch when ineligible.
- Evidence: `.tooling/0.3.2.0_B4/phase2-integrated.log`, successful Story runtime/server/coordinator probes including unchanged Task progress and Session start count.
- Runtime probe fixture checks Task 2/10; required real `kill_slimes` 2/3 remains a real-machine acceptance gate.

## Phase 3 (automated verification passed)

- Main owns immutable server candidates, all-identity actor collection, exact state/project revalidation, player/entity-bound one-shot choice tokens, expiry and non-destructive Region ambiguity.
- Native Worker owns only minimal vanilla chooser GUI, frame/selection codecs and DialogueNetwork registration, with a fixed token/index API. No client-provided Story ID is accepted.
- Candidate unit/coordinator evidence is distinct from real GUI display and physical NPC interaction acceptance.
- Evidence: `phase3-integrated-fixed.log` and `phase4-integrated.log`. Candidate/token/state/project isolation, stale choice rejection, non-destructive Actor/Region ambiguity and strict chooser codec probes passed. Main corrected a malformed codec test which incorrectly rejected index zero, and bounded chooser rows to available screen height.

## Phase 4 (automated integration passed; real acceptance pending)

- Production registration now constructs only Canonical Story/Session/Task execution. A dedicated Canonical Story event adapter replaces the dual-path adapter. Legacy dialogue gameplay packet registrations are retired.
- Story commands read/reset/start Canonical state only. Quest journal transport retains its DTO but projects exclusively Canonical Tasks. Worker command/journal changes were reviewed and adopted after integrated compilation and probes.
- Live Bridge snapshot and reload use Canonical state and B3 package-generation reconciliation. Legacy arbitrary-node Play Test now returns an explicit retirement response directing callers to canonical Story start/reset; it does not invoke the old state machine.
- `phase4-integrated.log`: compilation plus command boundary, Story coordinator, Actor arbitration, chooser codec and Task persistence probes passed. Exact reset probe preserves the other Story's encoded state.

## Phase 5 (automated integration passed)

- Identity contract: namespace `[A-Za-z0-9][A-Za-z0-9_-]{0,31}`, local ID `[a-z0-9][a-z0-9_.-]{0,62}`, full ID at most 96 ASCII characters. Case is significant; no trimming, case folding or silent normalization. Bare IDs remain explicitly compatible for old readers/migration.
- Safe path mapping: full IDs map to `x<lowercase ASCII hex namespace>/x<lowercase ASCII hex local>.json`; DGRS filenames use the two encoded segments joined by underscore. This also distinguishes case on Windows and avoids device-name collisions. Resource content remains authoritative.
- Main owns typed ownership migration, atomic transaction and integration; Worker owns matching pure Java/C# identity helpers and validation vectors only.
- Pure identity helpers adopted after Java probe and Studio tests. Main added typed rename map and Global/Custom/return planner, preserving prose/node/port identities and updating other Stories' references. Shared definitions may move together; incompatible owner destinations reject.
- File transaction adopted after review and injected write/delete failure tests. Main added Windows reserved-device and trailing-dot/space alias rejection. It provides in-process all-or-rollback with explicit rollback-failure reporting; no crash-durable guarantee is claimed.
- Evidence: `phase5-identity-java.log`, `phase5-transaction.log` (19 passing tests), `phase5-core-all.log` (390 Core tests), `phase5-settings.log` (25 settings tests). Later `phase6-core-final.log` passed 404 Core tests, including migration persistence, collision/stale-preview/failure rollback and layout preservation.
- GlobalNamespace settings preserve exact case and reject invalid writes without replacing existing settings. Typed final validation covers graph shapes, owned resources, declared references, Story Logic and Namespace policy. Migration supports one-unit undo while committed bytes remain unchanged.

## Phase 6 (automated integration passed)

- Worker packages own Studio repository CRUD/discovery and Java resource-reader/registry ID/path integration with the fixed helper contract. Main owns typed migration persistence, final validation, editor-layout reference preservation and packaging.
- `phase6-java-fixed.log`: namespaced loader, canonical graph/membership, project repository, package loader and B3 generation probes passed. Full logical IDs load from arbitrary safe physical paths; duplicate logical IDs reject; bare B3 filename diagnostics remain compatible.

## Phase 7 (isolated-copy automation passed)

- `phase7-copy-migration.log`: copied only project/resource/editor JSON from `E:/Java/MinecraftMod/RPGProject/project_test` to an isolated E-drive test directory. Migrated Story/Task `kill_slimes`, Actor Group `slimes` and other owned resources, reopened, exported both Stories and undid the migration with exact byte restoration.
- Original fixture JSON hashes were compared before/after and unchanged. The original project has not been migrated. Generated archives are under `.tooling/0.3.2.0_B4/migrated-packages`.

## Phase 8 (automated integration passed; real UI deferred)

- First use requires exact NameSpace input and first-project creation. Cancellation at either prompt persists no NameSpace. File menu offers settings and migration undo; Story graph context menu offers custom Namespace and return to Global.
- New canonical resources receive their owning Story's effective Namespace; exact full IDs are accepted in identity dialogs. Export filename uses the safe physical mapping.
- Global/custom migrations show a concrete preview before apply. A settings-save failure undoes the project migration. Theme/window persistence reads current settings to preserve later Namespace changes.
- `phase8-shell-workflow-fixed.log`: five workflow tests passed. `phase9-wpf-all.log`: complete WPF automated suite passed 452 tests. These are in-process tests, not live-window acceptance.

## Phase 9 (automated cross-language integration passed)

- Explicit external reference authoring records typed full IDs in referenced_resources without creating definitions. Export allows missing full-ID referenced definitions while requiring every owned definition; local shared resources retain their source bytes.
- `phase9-core-all.log`: 409 Core tests passed, one external-fixture test skipped because it has a separate environment-driven invocation. Includes missing external reference export and missing owned resource rejection.
- Runtime per-package validation permits missing explicitly referenced full IDs; owned resources remain required. The final installed-set merger strictly validates every dependency, resource kind and Story target before candidate publication. Session/Task references are checked per Story and per resource kind, so same-local resources cannot borrow another kind's declarations.
- `final-java.log`: `b4ExternalPackageReferencesProbe` and `b4ExternalArchiveSetProbe` passed. The latter loads actual Studio-produced archives: Consumer alone rejects; Provider+Consumer coexist with same local Story/Actor IDs; removing Provider or introducing different bytes under the same full Actor ID rejects and retains the previously accepted definitions.

## Phase 10 (automated full-ID path verification passed)

- Audited Canonical Story/Session/Task, registries, Nominator and transport/persistence paths. Full IDs remain intact in JSON, NBT and packet strings; 96-byte Session/Chooser ID fields cover the shared full-ID contract. Minecraft native entity registry-name normalization is separate from DGR Actor identity propagation.
- `dgrsPackageRuntimeProbe` now reads the canonical Story by parsed logical ID and derives the kill target from its typed Objective, rather than assuming flat filenames or bare `slimes`. Its generation fixture toggles the existing once/repeatable value; the current original `kill_slimes` fixture is already repeatable.
- Final namespaced `project_test` copy export passed Java Task execution, Nominator catalog codec, persisted Task progress, unchanged/corrupt/updated/removed package generation and offline replacement probes. These are automated runtime probes, not Minecraft client/world acceptance.

## Phase 11 (automated gates complete; real gates deferred)

- `final-java.log`: offline `build` plus all 49 registered JavaExec probes, including both actual-archive probes; BUILD SUCCESSFUL, 81 tasks (59 executed). The exact task list is `final-java-task-list.txt`.
- `final-core.log`: 411 passed, 0 failed, 0 skipped, including the environment-driven original-project isolated-copy test.
- `phase9-wpf-all.log`: 452 passed, 0 failed, 0 skipped. `studio-publish.log`: self-contained Windows x64 Release single-file publish passed with ProductVersion `0.3.2.0-B4`.
- Product/test diff whitespace check passed. Existing analyzer warnings and CRLF notices do not represent test failures.
- PLAN sections 48-53 have automated evidence covering repeat runs, active/once Start exclusion, actor selection isolation/staleness, canonical-only registration/commands, Namespace migration/undo/collision, physical paths, export/merge conflicts and B3 generations.
- PLAN sections 54-60 real-window/client interactions remain unexecuted in this continuation. Required next acceptance includes first-use UI, Global/Custom/return workflows, `kill_slimes` Run1/Run2/Run3, ACTIVE Start clicks, multi-Story one-NPC chooser, and Namespace coexistence/external reference gameplay.
- `phase4-build-fixed.log`: full Java build and selected boundary/identity probes passed after Main replaced wildcard imports in the new arbitration probe. Earlier `phase4-build-formatted.log` failed only these three Checkstyle AvoidStarImport rules.

## Prerequisite dependency audit (read-only, reviewed)

- Production execution to retire: `DarkGreyRpg.preInit` constructs QuestRuntimeService/StoryRuntimeService and StoryEventBus/StoryEventBridge alongside canonical managers. StoryEventAdapter posts legacy Actor/position events and invokes canonical Story handling; QuestEventAdapter and CanonicalTaskEventAdapter independently consume gameplay events.
- Commands: `CommandDarkGreyRpg` Story reset/state use legacy state; Story start/list/info retain legacy fallbacks. Session/Task command branches already use canonical managers.
- Presentation seam: `QuestRuntimeService` combines legacy Quest state with `CanonicalTaskLegacyJournalAdapter`; retained Journal presentation must become exclusively canonical-backed.
- Compatibility readers: StoryLoader, ProjectRepository and StoryPackageLoader retain old-format parsing. Studio CanonicalProjectMigrationPreview/Transaction already provide detached preview and transactional apply patterns.
- Identity/path assumptions: Java ProjectRepository/StoryLoader allow bare IDs with dots; canonical graph/membership/NPC/Item registries use a stricter bare-ID pattern. Studio ActorValidator and graph/membership repositories repeat these differing rules. GraphResourceRepository, CanonicalStoryMembershipRepository, StoryRepository, Java loaders and exporters assume flat `<id>.json` filenames. StoryPackageManifest rejects namespaced IDs; canonical content validation currently requires all references locally resolved.
- Worker audit changed no files; Main reviewed concrete production-registration and graph-repository anchors before adoption. Later phases must resolve these contracts together rather than applying independent string replacements.

## Delivery / acceptance

User steering (2026-09-08): real-machine acceptance is explicitly AUTHORIZED until B4 construction is complete. This supersedes the earlier prohibition. No repeat authorization is needed after tool recovery. Keep `USER_ACCEPTED=NO` and `STUDIO_FROZEN=NO` until explicit user acceptance.

The B4 local acceptance candidate is now present at the authoritative path:

- Path: `E:\Java\MinecraftMod\DarkGrey_RPG\dist\DarkGreyRPGStudio\DarkGreyRPGStudio.exe`
- ProductVersion: `0.3.2.0-B4`
- Size: `141608150` bytes
- SHA-256: `f2943b76cf5b3e22d6567c7a20e91e5c91212d7d79a1f6695a40570d83e8a854`
- The previous EXE was retained at `.tooling/0.3.2.0_B4/previous-studio/DarkGreyRPGStudio.exe`. The 2026-09-08 title fix was republished and the canonical EXE was launched with isolated acceptance settings; the refreshed native window list confirms the B4 title.
- Java build artifact: `build/libs/darkgrey_rpg-0.3.2.0.jar`, `893155` bytes, SHA-256 `1189af2800933317a9e60d1035d4e42c50f5d7cc2d5702665bc841ca41d0d46a`. This build includes the later case-sensitive ID extension, verified by Java production probes and an actual Studio-exported archive. Earlier server/client evidence belongs to the initial closure hashes below.
- Machine-readable checks: `.tooling/0.3.2.0_B4/studio-delivery.json` and `final-artifacts.json`.

## Resume boundary

Read this report, then the final B4 construction PLAN. Preserve the dirty checkout and unrelated historical PLAN removals. Do not commit, push, tag, publish a release, or migrate the original project by inference. Studio/Minecraft agent real acceptance is complete using the authorized traditional Windows route. The Computer Use plugin incompatibility no longer blocks B4.

On recovery, verify the above final hashes first and read Final closure before starting any further work. Retain `USER_ACCEPTED=NO` / `STUDIO_FROZEN=NO` until the user accepts. An automated PASS or local artifact promotion does not open the next release gate.

## Real-machine attempt 2026-09-08

- User explicitly authorized real-machine acceptance and continued B4 construction. Initial Studio and Java artifact hashes matched the prior candidate.
- Launched the authoritative Studio EXE with an isolated settings path under `.tooling/0.3.2.0_B4/real-machine-20260908`. The native accessibility tree confirms first-use Namespace onboarding, Minecraft-ID suggestion, validation text, and disabled Next while empty. This is limited native-window text evidence, not a completed visual/interaction gate.
- Native window listing exposed stale `0.3.2.0_A RC` in the title. Corrected the three visible title/about labels to B4. Release win-x64 self-contained publish and diff whitespace checks passed; that title-fix candidate was verified at the time and was later superseded by the final button-fit build. A fresh real window lists `DarkGrey RPG Studio 0.3.2.0_B4`.
- Screenshot capture failed twice after refreshing the window selection: `SetIsBorderRequired failed: 不支持此接口 (0x80004002)`. Text-only state capture succeeded, but set_value failed with `read UIA value read-only state: 所需属性不在 CacheRequest 中 (0x80070057)`; observed-focus typing produced no visible state/validation change; refocus click failed with `coordinate input geometry is unavailable`. No further blind input was attempted.
- Evidence: `real-machine-20260908/studio-first-use-accessibility.json`, `promoted-window-list.json`, `studio-publish.log`, and this report. No usable screenshot or completed namespace/project operation was obtained. No Minecraft client/server was started or modified in this attempt; original project and user settings were not migrated.
- The two isolated Studio processes started by this attempt were stopped after inspection/promotion. No acceptance settings file was created. The source change is limited to version labels; prior full test counts are historical evidence, not rerun claims for this new binary.
- PLAN 54-60 remain pending: first-use completion/restart; Global/Custom/return and references; actual old-project-copy migration/export; Minecraft Run1/2/3 and ACTIVE Start; shared Actor chooser; namespaced package coexistence and external Provider dependency/LKG gameplay.
- State: `BLOCKED_COMPUTER_USE`, `AGENT_REAL_MACHINE_VERIFIED=NO`, `USER_ACCEPTED=NO`, `STUDIO_FROZEN=NO`. Resume already-authorized real acceptance when native capture/input is functional; do not replace these gates with automated probes.
## Tool compatibility diagnosis 2026-09-08

- Host OS reports 10.0.19045.0. Microsoft documents GraphicsCaptureSession.IsBorderRequired as introduced in 10.0.20348.0 (UniversalApiContract v12): https://learn.microsoft.com/en-us/uwp/api/windows.graphics.capture.graphicscapturesession.isborderrequired
- Reset the Node kernel, reimported the supported @oai/sky API, selected the existing DarkGreyRPGStudio Explorer folder window, restored it from minimized state and retried capture. Explorer reproduces SetIsBorderRequired failed: 不支持此接口 (0x80004002). Thus the capture failure is independent of DGR and persists across a JavaScript-session reset. OS/API incompatibility is strongly supported; helper source was not available for verifying its internal conditional logic.
- The installed Computer Use plugin has only cached version 26.901.22334. Its documented API exposes no capture backend or IsBorderRequired compatibility option. No binary patch, helper protocol bypass, OS upgrade, or security configuration change was performed.
- Existing B3 minecraft-live-control.ps1 was reviewed as a possible alternative: it uses CopyFromScreen and Win32 input rather than the failing capture API. It has NOT been run here. Before reuse, remove its three overlapping mouse delivery mechanisms and enforce one observed action plus one new screenshot per step, unique target selection and foreground checks. Studio requires an equivalent bounded adapter. This is a proposed alternate automation route, not proof of a repaired tool or completed B4 gate.

## Traditional Windows route recovered 2026-09-08

- User directed selection of the traditional route if the current Computer Use plugin does not improve the work. Main chose this route because the broken plugin offers no demonstrated benefit for B4 over reliable native screenshot/input. This explicitly supersedes the earlier plugin-only method constraint for this acceptance work.
- Added local acceptance helper `real-machine-20260908/native-step.ps1`: target PID/application validation, active popup selection, foreground PID check, one action maximum, focused editable-control validation, one mouse delivery mechanism, fresh screenshots, and handling of a closed target window. Uses standard Windows UIA/Win32 and CopyFromScreen; does not modify the Computer Use plugin or OS.
- Real interaction now works: `traditional-01.png` first-use empty Namespace; `traditional-03.png` B4Live accepted and Next enabled; `traditional-04.png` first-project dialog; `traditional-08.png` isolated first project created; `traditional-10.png` real close/relaunch restores the project without onboarding.
- Isolated settings persisted global_namespace=B4Live and last_project under the B4 evidence directory. Original project and user settings remain untouched. PLAN 54 first-use/create/restart portion is now PASS; other PLAN 54-60 interaction gates remain pending.
- Current Studio acceptance PID is recorded in `traditional-process-id.txt`. Overall B4 is IN_PROGRESS, not AGENT_REAL_MACHINE_VERIFIED or USER_ACCEPTED. The previous BLOCKED_COMPUTER_USE entries above describe earlier attempts and are superseded by this recovery.

## Live acceptance progress 2026-09-08 (traditional Windows)

- Actual Studio old-project-copy migration preview (live-19), full-ID workspace and original connected graph (live-24/25), and real UI export (live-31) succeeded. Original project was hashed before copying; its path was never opened for migration.
- Actual three-story Global/Custom migration: C=CustomC:story_c references A-owned actor; global B4Next updated A/B and C's reference while preserving C identity (live-47/54/55). B changed to CustomB (live-60/61), stayed custom under global B4Final (live-66/67), then returned to B4Final (live-69/70). Direct structural/reference audit remains to be finalized.
- Live Minecraft uses isolated cloned world dgr-b4-live-20260908, empty legacy project and the actual Studio-exported B4Live:kill_slimes archive. Original server properties/config/client options are backed up under runtime-backup and must be restored after shutdown.
- Nominator physically bound the bartender and individual Slime instances to full B4Live IDs. Three real attacks advanced the initial task 0/1/2/3. ACTIVE Start at 2/3 preserved task state and did not restart Session (mc-47/49); the zero-candidate interaction fell through to vanilla trading as designed.
- Initial reward execution entered ERROR because the cloned world's old bare copper_coin binding did not define B4Live:copper_coin. This was an acceptance fixture omission, not a namespace fallback bug: PLAN 46 explicitly adds no world migration system. Bound a gold nugget to B4Live:copper_coin through the actual inventory Nominator (mc-65/68). ERROR restart through NPC successfully cleared old task state to 0 (mc-77/79).
- With full-ID item binding prepared, successful Run1 physically progressed 0/1/2/3 (mc-79/85/90/95), handed in to the bartender (mc-97), awarded 10 nuggets and TERMINATED (mc-98, server 05:13:26). Completed Task remains visible before next Start (mc-100). Run2/3 and package chooser/external dependency gates are still in progress.
- A bounded worker corrected observed first-use button clipping using adaptive MinWidth/Padding in NamespaceDialog.xaml. Main reviewed/adopted; final publish and live visual verification remain pending. No Java source changes were needed for the item-binding fixture correction.

## Real gates completed and defects corrected (2026-09-08)

- PLAN 54/55: final promoted Studio visually shows the complete onboarding button label (`studio-final-onboarding-fit.png`), restores the isolated migrated project, and preserves custom namespaces. Added B's explicit reference to A-owned B4Final:test_npc via UI, changed B to CustomBFinal, confirmed owned IDs changed while the referenced ID stayed B4Final:test_npc (`studio-b-custom-resources.png`), then returned B to global (`studio-b-return-done.png`). `live-migration-audit.json` confirms all 19 original files unchanged and all seven original Story/Session/Task graphs identical after normalizing only the expected namespace prefix; kill_slimes retains seven nodes and six connections.
- PLAN 56/57: successful Run1 zero/one/two/three, NPC handoff, ten-item reward, TERMINATED and retained completed journal result: mc-79/85/90/95/97/98/100. Run2 starts zero (mc-105), physical kills progress one/two/three (mc-112/117/124), handoff and another ten-item reward terminate normally (mc-127/128). Run3 starts zero (mc-132; server 05:22:32). Earlier ACTIVE Start at 2/3 remained unchanged (mc-47/49). Unbound child Slime kill did not count. Physical kills were actual mouse attacks; server summon/teleport/strength commands only prepared controlled targets. `repeat-runs-server.log` retains corresponding canonical events.
- PLAN 58 uncovered a genuine protocol defect: chooser discriminators 8/9 collided with Nominator registrations 8/9 on the shared channel. Actual click disconnected the client (`mc-139.png`, `chooser-disconnect-client.log`: chooser payload decoded as C2SNominatorEntityBind). Fixed chooser IDs to 15/16; added the complete 14-message registration regression `b4NetworkDiscriminatorProbe`. Offline build and probe passed. This source-level registry check supplements, rather than substitutes for, actual Forge decode verification.
- Repeated the identical NPC click with both rebuilt client and server: actual chooser now displays Consumer:story and Provider:story (`mc-fixed-06-chooser.png`). Selecting Provider terminates Provider while Consumer remains absent (05:53:58, mc-fixed-07). This proves actual server-side selection transport and isolation.
- PLAN 59: independently loaded Provider:guard and Consumer:guard share local ID guard but have distinct full IDs. Nominator displays both (mc-135/136); Provider binding succeeds (mc-137). With Provider still occupied, binding a second entity to Consumer:guard succeeds (mc-fixed-10/11). No local-ID collision or false occupied rejection.
- PLAN 60: Consumer archive owns Consumer:guard and only references Provider:guard; it contains no Provider Actor definition. Both packages loaded. Removing Provider's archive from installed-packages rejects the candidate merge and retains all previous fingerprints/Runtime (05:54:49). A subsequent real interaction still starts Consumer using the retained Provider Actor and terminates it (05:55:43, mc-fixed-08), while Provider's prior terminal state and kill_slimes Run3 0/3 remain unchanged. Restoring the provider yields all UNCHANGED and a successful reload (05:56:51).
- External live fixtures are explicit derivatives of Studio exporter test artifacts: `prepare-live-packages.py` changes both Start triggers to interact with Provider:guard and connects them to terminal nodes, leaving owned/reference manifests unchanged. This is disclosed fixture preparation; the actual GUI chooser, Nominator binding and runtime reload evidence above were obtained in the running Minecraft client/server. `chooser-fixed-server.log` and `chooser-fixed-client.log` preserve the fixed run.
- Final diagnostic-only correction: the failed reload's old generic text incorrectly claimed deleted definitions were unloaded and promised logs without logging validation errors. Updated the text to state invalid candidates did not take effect and added one warning per actual reload error. `final-diagnostics-build.log`: offline build and network registry probe PASS. This last change affects command diagnostics only; final server recheck and environment restoration are recorded below.


## Final closure (2026-09-08 06:08, Asia/Shanghai)

- Result: construction complete and `AGENT_REAL_MACHINE_VERIFIED`; PLAN 54-60 actual Studio/Minecraft gates passed as detailed above. `USER_ACCEPTED=NO`, `STUDIO_FROZEN=NO`. No commit, push, tag, release or next-version construction was performed.
- Final diagnostic build ran on the real server. At 06:06:24, removing Provider rejected reload with the precise logged error `project.content.actor.missing: Canonical membership references an unknown Actor: Provider:guard`. All three accepted fingerprints remained UNCHANGED. The corrected command response reports that the invalid candidate did not take effect. Restoring Provider and reloading at 06:07:13 succeeded with all packages UNCHANGED. Evidence: `real-machine-20260908/final-diagnostics-server.log`.
- Build boundary: repeatable Run1/2/3 client evidence predates the chooser network repair; lifecycle code was unchanged by that repair. Chooser, same-local-ID coexistence and external-reference/LKG client evidence used release JAR SHA-256 `cd849e3dab820eb08fe60e9fccd23493f9f2e50623aadc1c4a040de3d7f70178` / dev JAR `0fd027d13aa159aceee1cf1b60c38cd751ce904536a05e513bcfd89ae7cfe1ed`. The final subsequent Java change only corrected reload diagnostics in CommandDarkGreyRpg; final server verification covered this change. Full client gameplay was not repeated after that diagnostic-only change.
- Final release JAR: 893155 bytes, SHA-256 `3ea149e1f5082426dbbd5e100995bca21c3dc76d0f2f6309766d3dc0011323ca`; final dev JAR: 881800 bytes, SHA-256 `9f45c408b0fe759b524a04b4c09f4cd60ccf0dd92a143bb13377d248d1799502`. Final Studio delivery hash/version/size are in Delivery above and `studio-delivery.json`; the final EXE itself was visually exercised.
- Final offline Java build and network registration probe passed (`final-diagnostics-build.log`); final self-contained Studio publish passed (`studio-final-publish.log`). Earlier full-suite counts are historical checks, not claims that every suite was rerun after these bounded fixes.
- All acceptance-launched Studio/Minecraft client/server processes were closed; the final server saved its world and exited with BUILD SUCCESSFUL. Original server.properties, server darkgrey_rpg.cfg and client options.txt were restored byte-for-byte (`real-machine-20260908/environment-restoration.json`). The isolated `run/server/dgr-b4-live-20260908` world and evidence packages are retained for review. Original project remains unchanged across all 19 files; seven graph structures and typed properties are preserved in the migrated copy (`live-migration-audit.json`).


## User-directed authoring corrections (2026-09-08, after initial closure)

The initial closure above is historical. The user subsequently requested the following B4 changes; these supersede the earlier lowercase-local grammar and the earlier menu/textbox presentation.

- New Story creation displays NameSpace as fixed TextBlock text, not a read-only input. Only the local Story ID is editable. Display spacing is `NameSpace : localId`; persisted identity remains `NameSpace:localId` with no spaces. Pasting a full ID into the local field is rejected and cannot change the prefix.
- The Project menu no longer contains Story NameSpace or external-reference actions. Both sidebar Story and graph Story context menus capture the clicked Story and expose `修改 NameSpace…`, `使用全局 NameSpace`, and `添加外部资源引用…`. Unsupported legacy Story actions are disabled. UI terminology is NameSpace as explicitly requested.
- Full DGR local IDs now accept `[A-Za-z0-9][A-Za-z0-9_.-]{0,62}` with exact case sensitivity in Studio and Java Runtime. This applies to NPC, Actor Group, Item, Item Group, Story, Session and Task IDs through the shared identity contract. Namespace limits and 96-character full-ID limit remain. Existing lowercase IDs need no migration. Legacy bare compatibility remains its historical lowercase reader contract; current B4 authoring creates full namespaced IDs. Native Minecraft registry identifiers are a separate contract.
- The existing ASCII-hex physical mapping gives `Team:Guard` and `Team:guard` different files even on Windows. Studio repository tests save and reopen three case variants of Actors, Actor Groups, Items and Stories without collisions. A real Studio exporter test produces `namespace-ux/case-sensitive.dgrs` with case-distinct NPCs, Actor Groups, Items and Item Groups for Java loading/merge validation.
- Studio Core: 411 passed, 1 environment-driven historical migration fixture skipped; additional real-export test passed separately. WPF: 454 passed. Logs: `namespace-ux/core-case-tests.log`, `wpf-case-tests.log`, `case-export-test.log`. The first 12 focused tests predated the later uppercase extension; full WPF results supersede them.
- Actual final Studio window: fixed spaced NameSpace text and accepted uppercase local input (`ux-case-uppercase.png`); creating both `B4Final:CaseStory` and `B4Final:casestory` succeeds and both remain visible (`ux-case-coexist.png`). Project menu removal (`ux-project-menu.png`), graph menu labels (`ux-graph-menu.png`), and sidebar labels (`ux-final-sidebar.png`) were visually inspected. Screenshots are under `real-machine-20260908`; the sidebar screenshot predates only the uppercase grammar change. All live edits were in the isolated `namespace-ux/project` copy with isolated settings.
- These corrections have new artifact hashes. Initial B4 Minecraft gameplay evidence above remains historical; this correction uses Studio real-window checks and Java production probes, not a claim of repeated full Minecraft client gameplay. USER_ACCEPTED and STUDIO_FROZEN remain NO. No commit, push, tag or release.

- Final Java correction validation: offline `build` plus `dgrResourceIdProbe`, `namespacedResourceLoadingProbe`, `npcIdentitySavedDataProbe`, `itemIdentitySavedDataProbe`, `canonicalStoryChooserCodecProbe`, and `b4ExternalArchiveSetProbe` all PASS (`namespace-ux/java-case-build.log`). The log explicitly contains `STUDIO_DGRS_CASE_SENSITIVE_ARCHIVE=PASS`, NPC case-sensitive restart and Item Group case-sensitive restart. Main corrected two new probe defects (missing assertion helper and expected exception type) and formatter requirements before the final PASS; no product behavior was changed to satisfy those assertions.
- Final Studio self-contained win-x64 publish PASS; authoritative EXE visually tested as above. ProductVersion `0.3.2.0-B4`. Final correction artifacts:
  - `E:\Java\MinecraftMod\DarkGrey_RPG\dist\DarkGreyRPGStudio\DarkGreyRPGStudio.exe`: 141604054 bytes; SHA-256 `d71f74693e9155a13c5d8d2bb292ab4be740c13300e78008e3b5496b0678c9b3`.
  - `E:\Java\MinecraftMod\DarkGrey_RPG\build\libs\darkgrey_rpg-0.3.2.0.jar`: 893155 bytes; SHA-256 `1189af2800933317a9e60d1035d4e42c50f5d7cc2d5702665bc841ca41d0d46a`.
  - `E:\Java\MinecraftMod\DarkGrey_RPG\build\libs\darkgrey_rpg-0.3.2.0-dev.jar`: 881802 bytes; SHA-256 `89de01201b0e10ccd6155bb91f7115329dfa4b6c396512c801c1bf8f73b0a381`.
- Final `git diff --check` PASS. Acceptance Studio processes were closed; original user project/settings and Minecraft runtime configuration were not changed by this correction.


## Complete resource prefix and display correction (2026-09-08)

- The previous Story-only prefix correction was incomplete. All five resource identity dialog families now use one shared `ResourceIdInput` control: canonical NPC/Actor Group, Item/Item Group, Session/Task, generic Story/Dialogue/Quest, and legacy Actor create/import/rename. Full namespaced suggestions expose a fixed TextBlock prefix and a local-only edit field; exact IDs and case sensitivity remain intact. External-reference target selection remains a full-ID input because it intentionally selects another resource, not a newly owned ID.
- Sidebar and inspector formatting is now `[NPC_ID] NameSpace : local`, `[Item_ID] NameSpace : local`, and `[Group_ID] NameSpace : local`, using one presentation formatter. Persisted IDs do not contain these presentation spaces or labels.
- Live inspection caught the pre-existing long `Item Group ID` label overlapping the new prefix. Corrected it to `Group ID`; final actual shipped EXE popup screenshot `namespace-ux/targeted-group-fixed.png` confirms separated label, fixed NameSpace and editable local ID. `namespace-ux/targeted-story.png` confirms the final spaced sidebar format for all four Actor/Item kinds. Earlier `real-machine-20260908/prefix-npc.png` and `prefix-item.png` confirm the shared prefix layout in actual dialogs before only the final label/spacing polish. Actor Group/Session/Task and import variants are covered by the shared control and ViewModel tests; individual live-window interactions for every variant are not claimed.
- Native foreground capture was sometimes obscured by user activity. Final screenshots use process-scoped UI Automation and PrintWindow, which capture the actual application window without unrelated desktop content. All interaction stayed within the isolated `namespace-ux/project` and isolated settings; original project was not changed. Acceptance-launched Studio was closed after verification.
- Final WPF suite: 460 passed, 0 failed, 0 skipped (`namespace-ux/all-prefix-tests.log`). Self-contained win-x64 Release publish and `git diff --check` PASS. No Java changes or new Minecraft gameplay claim in this correction.
- Authoritative EXE: `E:\Java\MinecraftMod\DarkGrey_RPG\dist\DarkGreyRPGStudio\DarkGreyRPGStudio.exe`, ProductVersion `0.3.2.0-B4`, 141608150 bytes, SHA-256 `f2943b76cf5b3e22d6567c7a20e91e5c91212d7d79a1f6695a40570d83e8a854`. USER_ACCEPTED=NO; STUDIO_FROZEN=NO; no commit/push/release.

## Offline Collaboration / DGRS Reference & Import

本节记录 2026-09-08 本轮离线协作增量，取代前文“external-reference target selection remains a full-ID input”的旧 UI 描述。未扩展联网协作、账号、云依赖、Dialogue/Nominator 或 0.3.2.1。

### 内容、身份与所有权

- Reference 将验证后的 archive 原始字节复制到 `<Project>/references/*.dgrs`。文件名是存储实现；更新/移除按 manifest `package_id` 匹配，版本和内容 fingerprint 是元数据。重新选择同 identity 的包时原子替换工程内副本，重建 catalog 并重新解析打开的 Story；不同 identity 独立添加。工程恢复只扫描自身 references，不保留下载路径依赖。
- `OfflineProviderCatalog` / `OfflineProviderResource` 分别表示外部包和类型化资源。资源包含 kind、真实 DisplayName、大小写敏感 full ID、定义 JSON、source package/version、Project Origin、content fingerprint。重复 provider identity、重复类型化 full ID、坏包和路径链接会产生诊断；歧义资源不选择任意候选。
- Native authoring storage 和 provider catalog 分开。只读 Story 不进入普通 Native Story 列表；首页单独显示“引用故事包（只读）”。Story 内“项目内引用”仅列当前工程原生资源，“项目外引用”仅列 references provider；支持按名称、full ID、kind、provider 搜索，普通流程不再手填 Namespace + ID。
- Provider-backed Actor/Item/Graph 项携带只读状态和来源。外部图不能进入可写编辑器，重命名/删除入口禁用，Save/Save All 排除 provider graph。独立查看窗口展示来源、full ID 和只读定义；Story/Session/Task 另有禁用编辑控件的流程图页。Actor/Item 默认落在定义页，来源标签切换会清空上一标签的选择。
- 解析先查同 kind + full ID 的 Native，再查唯一 Provider；缺失显示原 full ID，不回退同 local ID。引用归属仍保存在 consumer membership 的 referencedResources。移除包前列出消费者，确认后只移除副本，保留 consumer membership。

### 导入与导出

- `OfflineStoryPackageImportService.BuildPlan` 完整解析 archive，筛选目标 Story 与其 owned Actor/Actor Group、Item/Item Group、Session、Task；external referencedResources 原样保留引用列表，其 provider 定义不复制。
- Import Plan 在任何 authoring 写入前检查实际逻辑 ID、所有权冲突、现有 provider 占用、目标路径和最终身份/Namespace 合法性。即使现有文件不是预期文件名，也按资源逻辑 ID 检测冲突。冲突逐项报告 kind、full ID 和原因；不覆盖、不改名、不改变 imported namespace。
- 应用前再次比较工程相关文件的状态摘要，拒绝过期 plan；所有资源、membership、Namespace policy 和可选 reference-copy 删除纳入 `NamespaceFileTransaction`，失败恢复原字节。引用转导入要求同 identity 且内容 fingerprint 匹配；先验证完整计划，在同一事务中写入 Native 后才删除 referenced copy。失败保留引用副本。
- 原 full IDs 保留，导入 Story 自动写入 Custom Namespace override，Global Namespace 变化不会将其 owned IDs 改成目标工程前缀。
- 新工程生成稳定 `project_origin_code`；兼容旧工程使用已持久化 project.id 作为来源值。导入不安装来源包 project.json。之后由目标工程导出的 archive 使用目标 Project Origin；Namespace 和 Origin 是不同维度。
- Java ProjectRepository 接受严格非空字符串 project_origin_code，保留原值，旧包 fallback 到 project.id。Runtime 对不同 Origin 声明相同 owned Namespace 发出诊断；referenced-only Namespace 不声明所有权。C# fingerprint 与现有 Java archive 内容算法一致。
- 用户菜单移除“迁移到 Canonical”，同时移除 dead command/alias/dialog wiring。旧格式工程在正常打开时走保留备份的内部兼容服务，再进入 Namespace 准备流程；不要求普通用户先操作内部 Canonical 菜单。

### 验证与实机证据

证据根目录：`.tooling/0.3.2.0_B4/offline-collaboration/`。本轮仅使用 `live-final` 隔离工程和独立 settings。GUI 操作通过此前用户选择的 traditional Windows 路线（进程限定 UI Automation / Win32 控件和实际窗口截图）；前台焦点不匹配时工具拒绝操作，后续文件对话框按目标进程和已观察控件定位。没有以 ViewModel 测试代替下列真实窗口证据。

| Gate | 真实 Studio 操作与证据 | 补充数据证据 |
| --- | --- | --- |
| A Reference | `referenced.png`、`provider-expanded.png`：工程内副本/独立区域；删除 downloads/B.dgrs 后重开。`final-actor-definition.png`、`final-readonly-item.png`、`final-readonly-story.png`：真实只读窗口。`picker-tabs.png`、`picker-external.png`、`consumer-created.png`：内外分离选择真实 B:boss。 | `readonly-controls.json`：图编辑控件 Enabled=false；ProjectA membership retains referenced B:boss，未增加 owned B:boss。 |
| B Import | `import-success.png`：B Story 和六类 owned resource 出现在普通工作区；`exported-B.png`：实际 GUI 重新导出。 | `import-audit.json`：Global=A、B:story override=B；实际 GUI archive `A-exported-B.dgrs` 的 Origin 等于 A 且不同于 B。owned-only/external-not-copied 另有 Core 测试。 |
| C Reference → Import | `fail-conversion.png`：不匹配 fingerprint 拒绝；`import-success.png`：匹配包成功。 | `conversion-fail-audit.txt`：失败前后工程所有文件路径和 SHA-256 相同；`import-audit.json`：成功后 references 中副本数为 0，A 原有 referenced B:boss 保留并可解析 Native。 |
| D Update | `updated-v2.png`：已打开 Story 自动显示“更新后的守卫首领”；`missing-visible.png`：同 identity 新版本删除 B:boss 后显示原 ID 缺失。来源文件名分别 B.dgrs、renamed-v2.dgrs、missing-v3.dgrs。 | 名称更新和 typed full-ID resolution 测试；不按文件名或 local ID 匹配。 |
| E Move | `moved-story.png`：整个 ProjectA 复制到 ProjectMoved，修改隔离 settings 指向新目录并由固定 dist EXE 重开后，仍能解析 B:boss。 | 工程中 references 副本随目录复制；原下载文件已不存在。 |
| F Conflict | `native-conflict.png`：再次导入已原生化的 B 包，界面明确列出 Story/Actor/Item/ItemGroup/Session/Task 的完整 ID 冲突。 | `conflict-audit.txt`：整包拒绝，工程所有文件路径和 SHA-256 无变化。 |
| G Menu | `menu.png`、`missing-menu.png`：项目菜单为新建故事、引用故事包、导入故事包、导出故事包、准备 Runtime 重载、验证项目。 | Core compatibility 保留，WPF migration command/alias 与专用旧窗口已移除。 |

其他实机证据：`remove-confirmation.png` 明确列出 consumer A:story；`removed-consumer.png` / `missing-validation.png` 显示移除后 B:boss 缺失，consumer 引用未删除。新图的真实窗口只读检查与包事务测试是本轮证据；没有把此前 Minecraft 实机 gameplay 结果冒充为本轮最终 Java hash 的实机验收。

测试记录：`core-all.log` 初次全量 424 通过、1 个既有环境条件测试跳过；`wpf-all.log` 初次全量 459 通过；`wpf-final-focused.log` 最后只读页修正和更新夹具后 5 通过。`java-final-build.log` 为 offline build + offlineOriginProbe 全部通过，包含实际 Studio archive Runtime 加载、跨语言 fingerprint 和五项 Origin/Namespace probe。早期失败日志保留用于追溯，最终结果以本节随后交付收尾记录为准。

### 最终修补、交付与状态

- 删除导入 Story 时，Item / Item Group 现与 Actor、Session、Task 一起进入删除计划、consumer blocker 和事务；只清除该 Story 的 Custom override。Shell 确认框列出全部 owned 资源。失败恢复资源与 policy 的精确字节，并保留原有删除结果检查。`OfflineImportedDeletionTests` 三项覆盖成功、消费者阻塞和注入故障回滚。
- 最终回归：`core-final.log` 为 **427 passed / 1 skipped / 0 failed**；`wpf-final.log` 为 **460 passed / 0 failed**。跳过项是既有 `B3KillSlimesCopyMigratesReopensAndExportsWithoutChangingSource` 环境条件测试，不代表本轮故障。`java-final-build.log` 为完整 offline build、格式/检查和七项 probe 全部通过；`diff-check.log` 为 `git diff --check` 通过。
- 实机发现并修复：Actor 默认空白图页改为定义页；来源切换清空旧选择；只读图自动适应完整节点范围；来源说明换行。首个单文件候选漏带原生库，已使用 `IncludeNativeLibrariesForSelfExtract=true` 修正并重新发布。早期候选与失败日志不是最终交付证据。
- 最终 hash EXE 的补充验收：`shipped-home.png` 显示 B 在普通 Native Story 列表；`shipped-custom-namespace.png` 显示保留 B；真实勾选“可重复”并保存得到 `shipped-edit-saved.png` 和 `imported-story-after-ui-save.json`（repeat_policy=repeatable）；`shipped-delete-confirm.png` 列出 B:boss/B:group/B:item/B:item_group/B:session/B:task；`shipped-delete-success.png` 与 `delete-audit.txt` 证明删除后无 B 资源或 Custom policy 残留。随后在同一最终 EXE 重新执行 Reference，`shipped-reference.png` 和 `shipped-readonly-fit.png` 证明仍正常引用并完整呈现只读图。
- `ProjectFinal` 是为最终编辑/删除验收复制的 ProjectMoved；准备时仅清除了 A 的 consumer 引用以允许删除 B。这是明确的隔离夹具准备，不是通过产品偷偷解除依赖。ProjectMoved 保留原有 consumer 和成功导入/实际重新导出的证据。A–G 完整流程中较早截图的代码差异仅为本节列出的删除修补与只读/布局修正；没有重新声称所有交互都发生在同一 final hash。
- 事务边界：使用现有进程内 all-or-rollback 文件事务，覆盖普通写入/删除失败；没有新增断电或进程强杀恢复日志，也没有把 Import 接入用户撤销栈。没有新增任何在线服务。只读图提供不可编辑的整体预览，完整字段也可在可滚动的只读定义页查看。

唯一权威 Studio 交付：

- 路径：`E:\Java\MinecraftMod\DarkGrey_RPG\dist\DarkGreyRPGStudio\DarkGreyRPGStudio.exe`
- 类型：self-contained Windows x64 Release，single file，含 native self-extraction。
- ProductVersion：`0.3.2.0-B4`
- 文件大小：**141684950 bytes**
- SHA-256：`5b85a01af4e9ac9f4a2e549a5d2ab6b325fec5e21a9b7900157d3efdaa2701c3`
- `publish-final.log`、`delivery.json` 与最终实际进程 56796 的窗口证据相互对应；关闭后重新计算 hash 未变化。验收启动的 Studio 已关闭。
- Java 最新构建位于 build/libs，摘要见 `java-artifact.json`；本轮未部署至 Minecraft client/server，因此本轮 Java 证据是构建与 Runtime loader/probe，不是新一轮 Minecraft 实机 gameplay。

本轮状态：**IMPLEMENTED / AGENT_VERIFIED / NEED_USER_VERIFICATION / USER_ACCEPTED=NO**。

工作保留在当前 canonical checkout；未 commit、push、tag、Release 或上传。既有未提交工作未重置；本节及证据用于用户验收和后续接续。

### 2026-09-10：项目 Namespace 与引用 UI 六项反馈修正

本节替代此前“软件全局 Namespace 自动同步项目”的描述。范围仍为 B4 离线故事包和相关 Studio UX；没有联网协作、Dialogue/Nominator UI 重做或 Git 发布。

- 引用包区域使用 220ms 布局高度动画，底部标题与候选列表一起移动。移除嵌套展开和侧栏资源详情，只保留包名按钮；点击后在右侧 Inspector 展示 dgrs 文件名、Story ID 和只读资源入口。原故事 Inspector 在此时隐藏，避免半透明主题下叠字。
- 项目 ID 独立验证 ASCII 大小写，不再复用 Actor 的旧小写校验；新建、保存、重开和 Java 项目读取保留大小写。Runtime 资源 ID 原有兼容规则没有扩大。
- Namespace 保存在每个项目的 namespace_policy.json。每次新建项目都询问，可预填上次创建所用值；打开已有策略的项目不比较或迁移到软件默认值。项目 Namespace 修改与撤销不改软件默认值。设置和撤销入口均位于“项目”菜单。
- 普通资源侧栏不再显示“项目内容”，其空标签折叠。引用角色/组、物品/组、会话、任务均仅显示“引用”。详细来源统一显示在 Inspector；会话/任务的 Editor Inspector 路径也覆盖。资源选择器来源文案改为 dgrs 文件名和 Story ID，不显示 Origin/指纹。

验证与证据（根目录 .tooling/0.3.2.0_B4/project-namespace-ux）：

- Core 427 PASS、1 SKIP（既有可选 B3 fixture 测试）；最终 WPF 462 PASS、0 FAIL。包括重复新建询问默认值、不同项目切换无文件改写、项目迁移撤销不写软件设置、大写项目 ID、引用包选择/精简来源、Session/Task Inspector 来源路径回归。
- Java offlineOriginProbe PASS，新增 PROJECT_ID_CASE_PRESERVED；离线 gradle build PASS，包含现有检查。没有新增游戏内验收结论。
- 最终 dist EXE 实际进程 4356：animation.json 展开、收起各记录 6 个不同标题位置（含中间位置）；provider-inspector-final.png 无叠字；resource-sidebar-final.png 展示六种资源的简短标记；session-inspector-final.png / task-inspector-final.png 验证对应来源详情。actor-inspector.png / item-inspector.png 来自最后一次仅修复 Session/Task 来源绑定前的同版候选。
- uppercase-input.png 和 uppercase-created.png：通过实际新建项目按钮创建 TestProject2，project.json 保留精确大小写。new-namespace.png 显示软件预填 DifferentServer；新项目策略为 DifferentServer，已有 LiveProject 仍为 A；project-namespace.png 从项目设置入口读取到 A。project-menu.png 和 file-menu.png 验证菜单位置。
- picker-final.png 仅验证空候选状态（fixture 中全部资源已被引用）；非空候选来源字段由工作流测试和源码检查验证，不把空列表截图作为非空卡片视觉验收。
- GUI 使用进程限定 UI Automation 与 PrintWindow；所有数据修改均在隔离目录。选择资源改为调用真实 Button Invoke 并检查 Inspector 结果；仅发送窗口鼠标消息的早期尝试未产生选择，不计为成功证据。
- 最终测试进程已关闭，既有用户进程 30616 未关闭。原在用 EXE 移至证据目录 previous-running.exe 保留，固定交付路径已放入新版；用户需保存并重新打开固定路径使用新版。

正式 self-contained win-x64 Release 交付：

- 路径：E:\Java\MinecraftMod\DarkGrey_RPG\dist\DarkGreyRPGStudio\DarkGreyRPGStudio.exe
- ProductVersion：0.3.2.0-B4
- 文件大小：141689558 bytes
- SHA-256：BD691412CC270A5AA102EFF5453099DBE8E0DB086AAB7BBAFA00A96BC6AA1E77
- 与 publish 候选哈希一致，见 delivery.json。USER_ACCEPTED=NO；无 commit/push/tag/Release。

### 2026-09-10：引用前缀、只读流程图与 Ctrl+Z 修正

- 侧栏引用标识改为名称前的 `[引用] `，不再单独占据第二行；适用于 Actor/Group、Item/ItemGroup、Session、Task。Inspector 增加醒目的“引用 · 只读”色块；本地资源的“拥有”提示移除。
- 引用包 Inspector 不再把 Story 自己列为资源按钮，独立入口改为“故事流程图”。其他资源按钮选中后在当前 Inspector 显示名称、类型、完整 ID 和只读定义；详情排列在资源列表上方，避免点击后需要滚到列表底部才能看到。
- 流程图窗口移除元数据/JSON 选项卡，仅显示只读图与返回上级入口。中键平移、滚轮缩放、100%/适应/重置保留。双击 session/task 节点按精确 resource_id 进入包内子图，可返回上级。引用资源打开路径携带同包图上下文。
- CanonicalGraphEditorView 增加独立只读模式：禁用节点内参数控件，屏蔽节点拖动、连线、剪刀、右键编辑菜单、键盘删除及相应图修改入口；不以禁用整张图替代只读。子图解析与显示使用分离的内存文档，没有保存入口。
- 删除“撤销上次 NameSpace 迁移”菜单。现有 Ctrl+Z/编辑→撤销优先处理当前编辑器历史；没有当前编辑器可撤销项时，撤销当前项目的最后一次 Namespace 迁移。保留项目匹配、未保存资源和事务文件一致性保护。

验证证据根目录：`.tooling/0.3.2.0_B4/reference-flow-ux/`。

- WPF 全套 465 PASS、0 FAIL（wpf-final.log）；最终仅调整 Inspector 详情顺序后，相关界面/工作流 35 PASS（final-ui-tests.log）。本轮未修改 Core/Java，不重复声称完成新的 Runtime 游戏验收。
- 回归覆盖只读视口可导航但拒绝修改、Story 入口与资源元数据分离、Session/Task 子图进入/返回、Ctrl+Z 迁移撤销及源文件恢复。
- 实际 EXE 进程 324 使用独立 live/ProjectA、ProjectB、settings。鼠标中键平移使节点 X/Y 从 523/233 变为 595/269；滚轮使宽度从 270 变为 303；随后左键拖动保持位置与宽度不变，见 mouse-results.jsonl 和 flow-pan/flow-zoom/flow-drag-blocked.png。真实双击进入会话及任务子图，见 flow-session.png、flow-task.png；返回上级按钮恢复故事图。
- 实际 Namespace A→UndoServer，随后发送 Ctrl+Z；全部项目文件的相对路径和 SHA-256 与迁移前完全一致（before-namespace.json、undo-result.txt）。专用菜单项已消失（project-menu.png）。
- 最终 EXE 进程 24948 再验证 Inspector 重排和各类引用标识，见 final-details.png、final-sidebar.png、final-item.png、final-session.png、final-task.png。reference-badge.png、native-inspector.png 验证角色引用色块与本地无“拥有”。前一个进程与最终版本仅相差 Inspector 详情块的排列顺序。
- 引用副本与原导出包 SHA-256 相同（provider-hash-check.txt）。测试进程均已关闭。既有用户进程 26372 未关闭，原在用 EXE 保留于 previous-running.exe；没有修改用户原项目或其设置。

正式 self-contained Windows x64 Release 交付：

- E:\Java\MinecraftMod\DarkGrey_RPG\dist\DarkGreyRPGStudio\DarkGreyRPGStudio.exe
- ProductVersion：0.3.2.0-B4
- 大小：141693654 bytes
- SHA-256：5BF35DC2A9A0B160AFD77CC6081B73B1574F18B13740973C88B0C80F50BED383
- 固定路径与 publish 候选哈希一致，见 delivery.json。USER_ACCEPTED=NO；无 commit/push/tag/Release。

### 剪刀按钮主题样式恢复

仅为 CanonicalGraphEditorView.xaml 中剪刀按钮的局部 Style 补回 BasedOn 默认 Button 样式，恢复与相邻按钮一致的深色圆角外观。保留原有只读隐藏条件。Release publish 和 diff --check 通过；实际固定 dist EXE 进程 27196 打开隔离故事后完成视觉检查（.tooling/0.3.2.0_B4/scissors-style/restored-style.png），测试进程已关闭。本次小范围样式修正未重跑完整测试套件。

正式 EXE：E:\Java\MinecraftMod\DarkGrey_RPG\dist\DarkGreyRPGStudio\DarkGreyRPGStudio.exe；ProductVersion 0.3.2.0-B4；141693654 bytes；SHA-256 8642810FEED67AD4FD8E13671456EC573634DDD996122E19ECBB0E807CF86128。USER_ACCEPTED=NO，无 commit/push/Release。

### 引用排版精简（白字标识、条目内折叠预览、选择卡片）

- Inspector 删除蓝色“引用 · 只读”色块；改为名称上方类型小字右侧、约一个空格间距的普通 `[引用]`。共享模板覆盖角色/组、物品/组、会话、任务；侧栏名称前的引用前缀保留。
- 引用包资源列表改为每条资源独立 Expander，预览紧跟所属标题展开和收起，不再在列表上方插入重复详情。显示资源类型与对应 ID；图资源保留查看只读流程图入口。
- 删除面向用户的原始 JSON/资源定义展开入口，非图只读查看窗口也不再显示 DefinitionJson。内部包内容与保存格式保持原有用途。
- 项目外引用卡片取消重复引用字样和蓝色标签，将小号 Actor/Item/Session/Task 放在名称旁；下方资源 ID、故事包文件名、Story ID 使用统一标签列与值列。项目内卡片同步简化，去掉“当前 Project”。来源字段独立传递，长值支持换行。

证据：.tooling/0.3.2.0_B4/reference-layout/。相关 WPF/离线工作流测试 36 PASS；Release publish 与 diff --check PASS。正式 dist EXE 实际进程 32948 使用隔离副本和设置，已操作条目展开/收起（resource-expanded.png、resource-recollapsed.png），核对 Inspector 行内标识（inspector-inline.png）及非空项目外引用卡片（picker-external.png）。未把空候选列表作为排版证据。测试窗口已关闭，未修改用户原项目。未重跑 Java/Core 或全套 WPF，也未扩大 Runtime 验收结论。

交付：E:\Java\MinecraftMod\DarkGrey_RPG\dist\DarkGreyRPGStudio\DarkGreyRPGStudio.exe；ProductVersion 0.3.2.0-B4；141697750 bytes；SHA-256 0FC9A581485389C2E27CDBB0EE89BB18ADBA29E6B16FF427AB37DD015AB8C9DC。固定路径与候选哈希一致。USER_ACCEPTED=NO；无 commit/push/Release。

### 2026-09-10：引用包 Inspector 改为四类资源文件夹

- 用角色、物品、会话、任务四个默认收起的文件夹替换每资源详情下拉；沿用资源库三角折叠样式。空文件夹保留且禁用展开；角色组归角色、物品组归物品。
- 展开后显示紧凑资源列表（名称与 ID），移除逐项详情下拉。双击会话/任务直接打开对应只读局部流程图，保留原有只读图浏览约束与包内子流程导航。
- 相关 WPF 测试 36/36 通过；新增四类归组、空文件夹、会话/任务图入口覆盖。实际运行权威路径 EXE，UIA 确认四类初始 Collapsed，真实鼠标双击分别打开守卫会话/守卫任务只读图并截图核对。git diff --check 通过。
- 证据：.tooling/0.3.2.0_B4/reference-folders/（tests.log、folders-collapsed.png、session-flow.png、task-flow.png、delivery.json）。仅使用隔离测试项目。
- 已提升 self-contained Windows x64 Release 到 dist/DarkGreyRPGStudio/DarkGreyRPGStudio.exe。ProductVersion=0.3.2.0-B4；141701846 bytes；SHA256=FF156FBD0F272E2772E812F7850F4239C948294B273B0EBE49C5C1BE78406865。
- USER_ACCEPTED=NO；未提交、未推送。

### 2026-09-10：独立引用撤销、资源重命名与间距修正

- 角色/物品（含组）右键菜单移除无操作效果的编辑/查看入口；仅会话/任务保留编辑或查看只读流程图。引用资源允许独立解除当前 Story membership，不删除 Provider 包或资源定义。解除后保留用户编写的图内容，若图仍使用已解除的资源，将显示未解析引用诊断。
- 故事包引用/移除和资源引用不再以保存全部编辑为前置条件；采用原地 membership/provider 刷新，保留当前 Story、原生 Session/Task 草稿与已有历史。移除原生引用时保留脱离列表的编辑器，避免丢弃尚未保存的内容。
- 引用/解除引用（包和资源）进入项目撤销/重做记录；Ctrl+Z/Ctrl+Y 与当前图历史按操作顺序衔接。文件事务比对预期字节并支持失败回滚，拒绝覆盖外部更新。资源关系移除为可撤销操作，不再额外弹出确认。
- 重命名窗口支持命名空间前缀固定、大小写敏感的资源 ID 本地部分，以及显示名称。角色/角色组、物品/物品组、会话/任务 ID 迁移更新项目内类型化引用和布局；拒绝原生或 Provider ID 冲突。项目 ID/Story ID 不在本次资源重命名范围。已导出故事包、服务器和其它项目不自动更新。修改 ID 需先保存未保存编辑；只修改显示名称和引用操作不受限制。ID 迁移完成后清空旧引用操作历史，避免用旧 ID 恢复文件。
- 创建角色/物品窗口使用内容自适应高度，卡片与按钮区域保留间距。右键菜单独立恢复紧凑行高（28 DIP，项间 Margin 0，垂直 Padding 2），没有把创建窗口的留白用于右键菜单。
- 验证：WPF 全量最终 468/468；Core 427 通过、1 跳过；新测试覆盖全部六类资源解除/撤销/重做、未保存 Story 保留、图编辑与引用历史顺序、类型化 ID 迁移/冲突/仅大小写修改。实际 EXE 启动通过，截图核对无效菜单入口已移除；桌面操作与截图未作为完整手工验收。
- 证据目录：.tooling/0.3.2.0_B4/reference-undo/。权威交付为 dist/DarkGreyRPGStudio/DarkGreyRPGStudio.exe，self-contained Windows x64 Release。
- ProductVersion=0.3.2.0-B4；141712086 bytes；SHA256=2625879CB78AEBCE1F1C89D25445F356F5AA12B4CAB9DDB46DFAD208F464C4FE。publish/ 与 dist/ 哈希相同。
- USER_ACCEPTED=NO；未提交、未推送、未发布 GitHub Release。

### 2026-09-10：撤回菜单固定高度造成的文字裁切

- 用户截图证明上轮 28 DIP 固定 Height 与 Fluent 模板内部布局不兼容，菜单文字被裁切；上轮关于紧凑菜单显示的判断不成立。
- 移除 FluentContextMenuItemStyle 的固定 Height，保留自动内容测量。未修改创建窗口间距或引用功能。
- 在本机隔离项目中实际打开故事右键菜单与角色右键菜单，逐张查看截图确认文字完整。证据：.tooling/0.3.2.0_B4/menu-clipping/home-menu.png、resource-menu.png。Release publish 与 git diff --check 通过；本轮是单项样式修复，未重复功能测试。
- 已提升 self-contained Windows x64 Release 到 dist/DarkGreyRPGStudio/DarkGreyRPGStudio.exe；ProductVersion=0.3.2.0-B4；141712086 bytes；SHA256=53B4BC3A5174A128594D0E7305B4E191CF52110396F0245BA1D0B3C420FBBE35，源/目标哈希一致。保留用户已运行实例，仅关闭隔离验证实例。
- USER_ACCEPTED=NO；未提交或推送。

### 2026-09-10：资源元数据编辑和首页故事资源文件夹

- 六类资源（角色、角色组、物品、物品组、会话、任务）统一提供“编辑”窗口，回填 ID、名称、标签，支持标签修改和清空；删除导出包/服务器提醒。会话、任务创建包含标签。旧裸 ID 未改变时仍允许编辑名称和标签。
- 会话/任务菜单分别为“会话图”“任务图”，另有“编辑”；Provider 编辑禁用。菜单自动高度与原有间距保留，无固定 Height。资源库与 Inspector 增加 Session_ID/Task_ID，Inspector 展示标签（空为 —）。
- Graph envelope 增加可选 tags 数组，缺失视为空，schema_version 不变；贯通 document/snapshot、复制与迁移、保存、包导入导出。Java 严格加载器接受并校验可选元数据，不改变 Runtime 执行语义。
- 同 ID 的名称/标签编辑保留未保存图和撤销历史；ID 变化仍要求先保存，沿用项目文件事务、类型化引用迁移和冲突检查。迁移请求同时写入名称和标签，冲突前后 JSON 字节保持一致。
- 首页原生故事 Inspector 保留标题和 Story ID，将描述/资源摘要/Flow 计数替换为四个默认收起的文件夹。按资源库次序列出原生/引用/缺失资源，引用前置 [引用]；空文件夹可见且禁用。底层故事描述等数据未删除。
- 原生会话/任务双击事件进入所属故事并打开局部图；跨故事保留脏工作区，保存全部包括保留工作区。缺失资源不打开空图，Provider 延续只读浏览。包引用刷新同步保留工作区和首页列表。
- 渲染初查发现首页 ListBoxItem 继承了黑色前景，已显式使用动态主题前景；后续 home-expanded.png 逐项确认四类名称、引用标识、ID 在暗色背景下为白字。用户同时指出此问题，最终成品已包含修复。
- 验证：Core 429 通过 / 1 跳过；WPF 478 通过；Java canonicalGraphResourceProbe=PASS；git diff --check 通过。新测试覆盖六类标签编辑/清空/回填、两类图创建标签与脏局部图保留、旧 envelope、快照复制、迁移、导入导出、ID 冲突字节完整、首页缺失和跨故事导航。已有 B3 文件依赖测试仍跳过。
- WPF 本地真实 Window 测试另行启用，Show 后渲染并验证首页折叠/展开、路由双击事件、Inspector、四种原生/只读图菜单、六类编辑窗口、两类创建窗口。菜单通过生产事件处理器检查，资源跳转通过路由事件检查。截图保存在 .tooling/0.3.2.0_B4/resource-metadata/。这不是原生鼠标点击验收：Computer Use 截图两次返回 SetIsBorderRequired 0x80004002，点击返回 coordinate input geometry is unavailable。
- WPF 并行回归中原有引用重做测试受 application-wide EditHistoryClock 干扰，已将使用多独立 Shell 的 OfflineShellWorkflowTests 标记 DoNotParallelize；最终全量 478/478。未更改生产撤销语义。
- self-contained win-x64 Release 已从 publish-final 提升到权威路径；源/目标哈希相同，启动到隔离项目成功。未提交/推送/发布 GitHub，USER_ACCEPTED=NO。
- 交付：E:\Java\MinecraftMod\DarkGrey_RPG\dist\DarkGreyRPGStudio\DarkGreyRPGStudio.exe；ProductVersion=0.3.2.0-B4；141725398 bytes；SHA256=81C5013962C5BC1F9AB388B99FFDE01F8F1E27E9FE3511DF012B916C71523557。

### 2026-09-10：资源名称与 ID 连排及整行尾部省略

- 资源库、原生故事 Inspector、引用包 Inspector 的条目改用单个 TextBlock 和内联 Run，名称/引用前缀/ID 连续左对齐；整行 CharacterEllipsis，只在右端省略。保留名称粗体、ID 小字、暗色主题前景与选中态；完整资源信息仍可悬停查看。
- Run 的只读数据绑定明确使用 OneWay，避免其默认双向绑定引发只读属性异常。未调整菜单、模型或保存行为。
- 使用隔离项目中的酒馆老板和长 ID 检查 WPF 窗口渲染，确认名称完整且 ID 尾部省略。证据：.tooling/0.3.2.0_B4/resource-row-trimming/home-expanded.png 和 resource-library.png。窗口渲染/事件检查 1/1、相关视图测试 29/29、Release publish 和 git diff --check 通过。
- 已提升 self-contained win-x64 Release 到权威 EXE；ProductVersion=0.3.2.0-B4，141725398 bytes，SHA256=53C08B9EB360661BFF1E140E1190EAC2E9609CFB69D577543680E004B46E34A2，源/目标一致。USER_ACCEPTED=NO；未提交/推送。
