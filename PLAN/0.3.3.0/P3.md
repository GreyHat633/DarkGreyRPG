# P3 — Task 阶段奖励

状态：P3 自动化验证通过，阶段包已交付；继续 P4。USER_ACCEPTED=NO，尚未实机验收。

审计：现有 PlayerRpgSavedData.claimReward 只更新内存集合并 markDirty；无法单独充当物品发放前后的落盘屏障。Task snapshot 当前没有阶段奖励回执。保留既有基础，新增 Task 专用事务。

设计：Logic In only 的 reward 节点包含 item / xp signed entries；每实例、节点独立记录 pending / granted。玩家变更先形成完整结果镜像并原子持久化，再应用镜像与玩家持久回执。恢复只重放已提交的结果镜像，不再次计算增量。正数物品无法完整装入时保持待发，不产生不可恢复的世界掉落。

共用事务还将补齐 P2 提交物品在玩家/Task 分开保存时的恢复窗口。开始节点保持冻结。

已实现共用事务：提交先在副本计算精确扣除，持久化玩家结果镜像及累计回执，然后应用并验证玩家存档，最后更新 Task。Task 存档落后时，采样自动补记提交目标完成而不再次扣除。记录保留至下一事务覆盖，覆盖前必须恢复旧记录。玩家存档验证失败时断开该玩家连接，保留记录供重连恢复；不会回滚已经持久化的决定。进程崩溃探针通过；不宣称已验证断电、磁盘损坏或任意第三方存档实现。

Reward UI 支持多项、Item 下拉、XP、signed amount、删除、撤销，禁止将 ItemGroup 当作发放物品。Task metadata 在命名空间资源复制时保留；Reward Item 使用现有重命名函数，未改变身份算法。

验证：Core 433 通过、1 外部 fixture 跳过；WPF 488 通过；最后 ItemGroup 排除变更的奖励编辑器测试再通过；build + 60 个 Java 探针全部通过（evidence/p3/java-build.log）。P3 scope guard 通过。覆盖持续 True、True/False/True、NBT 重载、不同 placement 隔离、等待奖励后结算、正负/零物品和 XP、背包满、日志提交与玩家保存之间的故障恢复。

权威 Studio：E:/Java/MinecraftMod/DarkGrey_RPG/dist/DarkGreyRPGStudio/DarkGreyRPGStudio.exe

- ProductVersion：0.3.3.0-P3
- 大小：141735644 bytes
- SHA-256：A8E5212FF29290E3F01CE70F88759401D8E7406481EBFB3C38817EA5C4C931E6
- Runtime：build/libs/darkgrey_rpg-0.3.3.0-P3.jar
- Runtime SHA-256：7C4F0D1A669E0B7477E7A954C5D15BA15A843F015342389D1B5C2BAC2E2C8BBE
