# P4 — Story 执行

状态：P4 自动化验证通过，阶段包已交付；继续 P5。USER_ACCEPTED=NO，尚未实机验收。

工程选择：保留 persisted type=action，作者名为执行；经验为 XP points；生命直接 clamp(current+delta,0,max)，不转为伤害事件；传送使用 dimension_id / x / y / z，保留玩家原 yaw/pitch。高级命令来自已安装 Story 内容，以该玩家位置和名称、最高权限等级 2 的服务端命令上下文执行，一节点一行命令。以上为明确实现语义，不冒充用户另行确认。

开始节点不改。P3 阶段包及自动验证已交付，继续扩展本阶段原生执行能力和 BUFF 查询。

BUFF duration_delta 使用秒，等级使用 1-based；零不操作，结果持续时间或等级 <=0 移除。结果超过 1.7.10 NBT 可持久化范围时报错，不截断为错误等级。按已加载 Mod 的 owned packages 或明确命名空间确定来源；无法唯一确定来源或同一来源出现重复内部名称时拒绝解析。启动构建 lookup cache，执行不反复扫描。/dgr buff export 固定写入世界 data/dgr_exports/buffs.txt，UTF-8。

高级命令为权限 2 的命令上下文，带玩家名称、坐标、世界，不是强转 EntityPlayer 的命令发送者；要求玩家参数的原生命令应显式指定目标。关闭高级选项会将命令节点切回消息发送，可撤销。任意原生命令内部行为不被宣称具有全局回滚能力。

验证：Core 434 通过、1 外部 fixture 跳过；WPF 489 通过；最终补充字段修复后执行 Inspector 测试再通过；build + 61 个 Java 探针全部通过。P4 scope guard 通过，Start 配置和原网络 discriminator 保持不变。

权威 Studio：E:/Java/MinecraftMod/DarkGrey_RPG/dist/DarkGreyRPGStudio/DarkGreyRPGStudio.exe

- ProductVersion：0.3.3.0-P4
- 大小：141749980 bytes
- SHA-256：B4A6AD7AD7773D1C82BC7B56FDF4F06884D5722B12757E71094F44D96565C290
- Runtime：build/libs/darkgrey_rpg-0.3.3.0-P4.jar
- Runtime SHA-256：AFED8700DEE46C7BA25B5D9FB8EDEAFB832101137F47CE47EA7E8A99C9072010
