# 0.3.3.0 P1 — 旧独立触发器节点清理

日期：2026-09-12。分支：`codex/0.3.3.0`，基线：`82c6443238e41ce820f076e09d65d3cb26bb2236`。

用户已明确：只删除旧独立触发器节点，现用开始节点配置不得改动。P0 的“需要重新定义激活契约”判断作废；P0 文档和首次扫描保留为历史证据。

## 本阶段结果

- 删除 canonical Story 的 `interact_actor`、`enter_region`、`enter_story` 定义，以及独占的中途等待、事件续行和跨 Story 转移执行分支。
- Studio 创建、加载、保存、导出和旧数据迁移不再接受或生成这三种独立节点。Java 文件加载、包内存对象加载和 Runtime 均拒绝它们。
- 删除项目视图和删除规划中来自独立 `enter_story` 的关系生成。保留视图消费的空关系 DTO 接口，未重构项目图基础框架。
- Start 的 `triggers`、`repeat_policy`、既有兼容读取、NPC / 区域 / Logic / 命令启动保留。Task Objective `interact_actor` 保留。
- 旧快照的区域/转移空槽仍保持原存储布局；任何非空旧独立节点 payload 均被拒绝，已删除的等待枚举和执行分支不会恢复。

## 验证

| 检查 | 结果 | 证据 |
| --- | --- | --- |
| Core Release | 427 通过、1 个外部样本测试跳过、0 失败 | `evidence/p1/p1-core.trx` |
| WPF Release | 485 通过、0 失败 | `evidence/p1/p1-wpf.trx` |
| Java 完整 build + 58 个注册探针 | PASS，58/58 探针通过 | `evidence/p1/java-build-probes.log` |
| P1 Scope / 自测 | PASS；Start 定义不变，独立节点注册/执行扫描为 0 | `evidence/p1/scope-p1.log`, `scope-selftest.log` |
| Start 专属源码与历史 freeze guard | 与 exact baseline 一致（统一换行比较） | `evidence/p1/preserved-start-and-freeze.json` |
| 当前项目只读复核 | 19 条清单记录，文件差异 0，无待转换的三种独立节点 | `evidence/p1/legacy-project-inventory.csv`, `current-project-diff.json` |
| 自包含 win-x64 Release 打包 | PASS | `evidence/p1/studio-package.log` |

全量探针首次复跑暴露一个 B4 样本仍使用独立 Actor 节点，同时发现内存对象可绕过文件加载检查。已补内存拒绝校验，样本改用现用 Start Actor 配置，保留未声明 Actor 引用必须失败的断言，并新增三种旧节点的内存加载负例。修复前日志保留在 `java-build-probes-before-package-fix.log`。

Core 跳过项为显式依赖 `DGR_B4_MIGRATION_FIXTURE` 的外部样本测试，不计为通过。WPF 全量测试后只增加了迁移入口对 null 节点的防护，Core 全量已复跑；未改 WPF 行为。

## 可运行阶段包

- Studio：`E:\Java\MinecraftMod\DarkGrey_RPG\dist\DarkGreyRPGStudio\DarkGreyRPGStudio.exe`
- ProductVersion：`0.3.3.0-P1`
- 大小：`141720284` 字节
- SHA-256：`EA558439D52047F4E40A51C65BBE3F59772B7D4C94C72888E96FDE7F72E1BB8C`
- Runtime：`E:\Java\MinecraftMod\DarkGrey_RPG\build\libs\darkgrey_rpg-0.3.3.0-P1.jar`
- Runtime 大小：`971315` 字节
- Runtime SHA-256：`2B8990B5F79F4426758E7B476F8AF777A39ADABAB5311F5D25EF832707F9E121`

P1 前的 Studio 备份：`.tooling/0.3.3.0/studio-before-p1/`。
源码改动清单：`evidence/p1/source-whitelist.txt`。原有历史文件删除未处理；未提交、推送或创建 Release。

```text
P1_IMPLEMENTATION=COMPLETE
P1_FINAL_VERIFICATION=PASS
0.3.3.0_CONSTRUCTION=IN_PROGRESS
0.3.3.0_USER_ACCEPTED=NO
```

本报告为实现与自动验证证据，不包含本轮真人或游戏实机验收。P2 的物品提交入口、收集锁存和区域采样仍待用户定案；它们与本次开始配置无关。
