# 0.3.3.0 实施选择与用户确认

2026-09-12 用户已明确：

- 只删除旧独立触发器节点，现用开始节点配置不能动。
- DESIGN-GATE-D：标题播放结束后再继续流程（blocking）。此前倾向非阻塞的计划文字不再作为实施依据。
- DESIGN-GATE-F：同时支持 MP3/WAV 导入，导入时转换为 OGG；OGG Vorbis 也可直接导入项目。

2026-09-13 用户确认 DESIGN-GATE-E：死亡或断线停止音频；复活或重连恢复当前音乐，不重播当前台词语音。

A/B/C 采用工程默认值继续施工，详见 P2.md；未冒充用户确认。
