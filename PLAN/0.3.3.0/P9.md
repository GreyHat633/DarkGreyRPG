# P9 集成与负面检查

自动检查完成，真实游戏恢复和性能观察仍列入 P10 未完成项。

- 完整 Java build 和 65 项探针通过：.tooling/0.3.3.0/p9/java-all.log（2m34s）。
- Studio 实际导出的 media_story 与 presentation_story DGRS 由 Java 加载，并执行音乐/画面/台词链路；没有用手写包替代跨端导出检查。
- PNG 校验块边界、CRC、尺寸及结束标记；OGG 校验页边界、CRC、Vorbis 首包及结束标记。JPEG 包级检查签名/结束标记，导入时另由 FFmpeg 完整解码，客户端图片解码在后台进行。
- 修正旧 PNG 测试样本的错误 CRC；损坏媒体和 ZIP 中额外不可达媒体被拒绝。
- 目录包在分配前检查单条目 64 MiB 和总量 256 MiB，并限制流式读取过程中增长的文件。ZIP 媒体解压也检查实际输出上限。
- 分块临时文件使用文件锁，启动回收跳过活跃下载；队列拥堵时，后台工作会清理退休状态的文件句柄。
- MP3/WAV 转为 48 kHz 双声道 Vorbis；单/双声道 Vorbis 可直接导入，多声道 OGG 会转换，保留原始源文件副本。
- 实际 FFmpeg 测试覆盖 MP3/WAV/OGG、PNG/JPEG、重复导入和外部源文件删除后的独立性。
- 最终 Java 定向复测与构建见 .tooling/0.3.3.0/p10/java-final.log，纹理解码总量限额也有探针断言。

范围检查：SCOPE_GUARD_Final=PASS；开始节点配置未改，旧独立触发器定义/运行路径和 Narration 运行路径均为 0。最终数据见 P10.md。
