# P5 — 台词、Actor 头像与逐句语音作者配置

2026-09-13。阶段客户端已更新，整版 0.3.3.0 尚未完成。USER_ACCEPTED=NO。

- 独立 narration 注册项、Session Runtime 分支和网络帧枚举已移除；无说话角色使用 line。保留 LINE=0、CHOICE=1。
- speaker_actor_id 可空；头像变体与 voice_ref 属于当前句。切换角色原子清除变体，撤销恢复角色与变体；语音保持独立。
- 对话姓名下有 1 px 分隔线，正文仅在渲染层加「」；无角色时不显示姓名、占位头像、分隔线或引号。真实图片绘制与媒体传输留在 P6/P7。
- typed Actor schema 4 增加 default_portrait_ref、portrait_variants，保留 NPC_ID/Group_ID 身份含义。旧 schema 3 作者文件不再直接加载；旧 schema 1/2 导入领域尚保留。
- Actor 保存、WithId、Document dirty tracking 和编辑器撤销保留头像。运行时和本包导出校验变体存在；跨包 Actor 在服务端已安装集合中解析。
- Studio 加入 Actor 默认头像/变体导入、移除、命名入口及台词语音导入、移除入口。
- 提前接入 P6 项目媒体导入：MP3/WAV 转 OGG Vorbis，OGG Vorbis 验证后直接收纳；PNG/JPG 导入验证。源文件和运行时文件均在项目内，使用 SHA-256 内部引用。
- 当前工程默认导入上限为源文件 256 MiB、音频 3600 秒、图片 33554432 像素；不是用户确认值，也不是原 PLAN 中讨论的 50 MB/192 kbps。需在 P6 完成时复核并完善错误 UX、取消、超时与边界测试。

## 验证与交付

- Core 全量 437 PASS，1 既有 skip；WPF 全量 490 PASS。
- 实际 MP3/WAV→OGG、重复导入、外部文件删除后可用、OGG 直接收纳、损坏指纹拒绝通过。
- P5 范围守卫 PASS，Start 注册项 UTF-8 逐段完全一致；修复 Windows PowerShell 误读无 BOM UTF-8 的检查脚本问题，没有放宽 Start 比较。
- Java build + 61 个探针全部 PASS（java-final.log）。新增无角色 Runtime 与当前句媒体包往返向量也已单独通过（session-final.log）。
- 开发项目 3 个 Actor 迁移前均经过生产 ActorSerializer/Validator 校验；备份及前后哈希：`.tooling/0.3.3.0/p5/project-migration-applied.json`。没有迁移 Start 文件。
- 历史 DGRS 未覆盖，新 Actor schema 4 的测试包位于 `.tooling/0.3.3.0/p5/fixtures`；新探针参数使用 `scripts/0330-p5-probes.gradle`。

固定客户端：`E:\Java\MinecraftMod\DarkGrey_RPG\dist\DarkGreyRPGStudio\DarkGreyRPGStudio.exe`

ProductVersion：`0.3.3.0-P5`

Bytes：`141791964`

SHA-256：`4C5B17B163C2D16C2D6E3D833D997BB8A640016EBBCB59F0BA2057247E9DCD52`

依赖目录 `media-tools/ffmpeg` 包含 ffmpeg.exe、ffprobe.exe、LICENSE、README.txt，调用独立进程；版本为实测 `9.0-full_build-www.gyan.dev`。README 记录上游源码 commit d32b387f2b。本阶段未公开发布任何包。P6 需完成可复现依赖准备和发行资料审计。

## 后续必须继续

P6 reachable media packaging、完整性/负载验证、客户端分块传输与缓存、Undo-safe GC；P7 music/screen/voice lifecycle；P8 按用户决定阻塞标题；P9 集成恢复性能；P10 实机与视觉验收。当前媒体引用尚不能随 DGRS 到客户端播放，不得把本阶段报告解释为整版完成。
