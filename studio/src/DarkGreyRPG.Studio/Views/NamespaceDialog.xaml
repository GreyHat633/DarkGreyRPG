<Window x:Class="DarkGreyRPG.Studio.Views.NamespaceDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="NameSpace 设置" Width="560" Height="360" MinWidth="500" MinHeight="330"
        ResizeMode="NoResize" WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        Background="{DynamicResource ApplicationBackgroundBrush}"
        Foreground="{DynamicResource TextFillColorPrimaryBrush}">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock FontSize="20" FontWeight="SemiBold" Text="NameSpace 设置" />
        <TextBlock x:Name="FirstUseText" Grid.Row="1" Margin="0,6,0,0" TextWrapping="Wrap"
                   Foreground="{DynamicResource TextFillColorSecondaryBrush}" Visibility="Collapsed" />
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="110" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock VerticalAlignment="Center" Text="NameSpace" />
            <TextBox x:Name="NamespaceTextBox" Grid.Column="1" Padding="8,5"
                     AutomationProperties.Name="NameSpace"
                     TextChanged="NamespaceTextBox_OnTextChanged" />
        </Grid>
        <TextBlock Grid.Row="4" Margin="110,7,0,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                   TextWrapping="Wrap"
                   Text="建议使用自己的 Minecraft 游戏内 ID 作为 NameSpace，方便其他用户识别，并降低不同作者创建重复 ID 引发冲突的风险。" />
        <TextBlock Grid.Row="5" Margin="110,7,0,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                   TextWrapping="Wrap"
                   Text="仅支持 ASCII 字母、数字、下划线和连字符；最长 32 个字符，且必须以字母或数字开头。" />
        <TextBlock x:Name="ValidationText" Grid.Row="6" Margin="110,10,0,0"
                   Foreground="{DynamicResource SystemFillColorCriticalBrush}" TextWrapping="Wrap" />
        <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Width="88" Height="32" Margin="0,0,8,0" IsCancel="True" Content="取消" />
            <Button x:Name="ConfirmButton" MinWidth="112" Height="32" Padding="12,0" IsDefault="True"
                    IsEnabled="False" Style="{DynamicResource AccentButtonStyle}"
                    Click="ConfirmButton_OnClick" />
        </StackPanel>
    </Grid>
</Window>
