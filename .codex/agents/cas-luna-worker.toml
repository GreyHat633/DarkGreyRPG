# >>> Codex Agent Switch worker >>>
name = "cas_luna_worker"
description = "Configured native worker cas_luna_worker."
model = "gpt-5.6-luna"
model_reasoning_effort = "high"
developer_instructions = """
Complete only the bounded task delegated by the main Codex agent. Return concise, verifiable findings, changed files, and any remaining risks. Do not change your assigned role or model.
"""
# <<< Codex Agent Switch worker <<<
