package darkgrey.rpg.story.canonical.runtime;

/** Persisted lifecycle of one canonical Story execution. */
public enum CanonicalStoryStatus {
    ACTIVE,
    TERMINATED,
    TRANSFERRED,
    ERROR
}
