---
name: studio-node-ui
description: Design or change DarkGrey RPG Studio UI text hierarchy, node property editors and Inspector layouts, including helper text, input placeholders and repeated multi-field module cards.
---

# Studio 节点属性 UI

修改 Studio 节点内编辑区或 Inspector 时，先判断内容结构，再选择分组方式。

## 文字层级（适用于 Studio UI）

- 说明性文字必须比正常字段文字小，使用浅灰色、正常字重；例如字段解释、操作提示和行为说明。沿用当前 `AuthoringText.IsHelp`：11 DIP、自动换行，深色主题为 `#B8B8B8`。
- 待输入的引导文字（placeholder）使用深灰色，沿用当前占位文字机制和 `AuthoringText.BindPlaceholder`：深色主题为 `#909090`。引导只在未输入时出现，不作为字段实际值保存；输入后使用正常正文颜色。
- 字段名称和用户已输入的内容使用正常正文样式，不套用说明或占位样式。修复黑色文字时也不得把说明、引导和正文全部改成白色。
- 复用 `studio/src/DarkGreyRPG.Studio/Views/Graph/AuthoringText.cs` 的语义样式与主题适配，不在各个页面重复硬编码颜色。浅色主题沿用当前适配：说明 `#666666`，占位 `#888888`，保证可读性。
- 同一种说明或引导在节点内、Inspector、其他 Studio 编辑区保持一致；新增或修改 UI 时检查空输入、有输入和主题切换后的文字层级。

## 多模块卡片

- 单个节点包含多个独立模块，且每个模块包含多个要素（例如名称、类型、数量、目标等字段）时，每个模块使用一张卡片，明确模块归属。
- 典型适用对象：开始节点的启动条件、奖励节点的奖励条目。即使当前只有一条，也保留同一种卡片结构，以支持后续添加。
- 卡片包含清楚的模块标题、可见边框、内边距和模块间距；删除和已有的排序操作归属于各自卡片。列表标题显示模块总数。
- 以 `studio/src/DarkGreyRPG.Studio/Views/Graph/RewardEntriesEditor.xaml` 为现有视觉参考：圆角 4、内边距 8、模块间距 10、清晰的灰色边框和主题卡片背景。调整时保持深浅主题下可辨识。
- 节点内编辑区与 Inspector 保持同一种卡片设计、字段顺序和操作语义；优先复用组件或样式，避免两处逐渐分化。

## 角色、物品资源拖放

- 所有角色和物品选择框必须支持从左侧资源库拖入对应资源直接选中，新增节点或抽取共用组件时也必须保留此能力。节点内编辑区和 Inspector 都要接入。
- 复用 `ResourceSelectorDrop.Kind` 选择框行为，按稳定资源 ID 匹配当前下拉框允许的资源；不能按显示名称匹配，也不能绕过个体、资源组、引用资源和只读状态的现有限制。
- 精确更新鼠标落入的选择框。多条奖励、多条启动条件必须只修改命中的那条；同一个节点有物品和角色字段时分别识别，不能默认写第一条或其他字段。
- 错误资源类型、缺失资源、不在候选范围内的资源必须拒绝，且不得继续冒泡到画布后误改其他参数。保留下拉选择、保存、撤销和重做行为。
- 验证实际拖放后的赋值和持久化结果，覆盖节点内与 Inspector、多模块的第二条、同名不同 ID、错误类型拒绝、保存重开和撤销；不能只检查允许拖入的鼠标图标。

## 不适用的情况

- 不因“有多个条目”就自动拆卡片。每项只有一句话、一个名称或一个简单列表项时，保留紧凑列表。
- 当前选择、结算、画面节点保留紧凑设计，不在本规范下自动卡片化。
- 不把单个模块的每个字段拆成独立卡片；多个端口也不等于多个属性模块。

验证时同时查看节点内和 Inspector 的单条、多条及增删后的状态，确认模块边界清楚、操作归属明确、没有多余空白或文字裁切。视觉改动不得改变节点数据、端口或运行语义。
