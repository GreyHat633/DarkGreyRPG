# DarkGrey_RPG 0.3.0.0 Development Baseline

Recorded on 2026-08-28 before Phase 1 implementation.

## Source state

- Commit: `9be86aa` (`Fix Studio dialogue and quest draft UX`).
- Baseline contents: Studio 2.1.3 and Runtime 0.5.0.
- Development branch: `codex/0.3.0.0-flow-first-authoring`.
- The pre-existing deleted files under `examples/phase4_project` and untracked
  files under `PLAN` are user work and are excluded from this development slice.

## Automated baseline

| Surface | Result |
| --- | --- |
| Studio Core | 90 passed, 0 failed |
| Studio WPF | 186 passed, 0 failed |
| Runtime Gradle build | passed |

Studio Core command:

```powershell
E:\Java\dotnet-sdk-10\dotnet.exe test studio\src\DarkGreyRPG.Studio.Tests\DarkGreyRPG.Studio.Tests.csproj --no-restore --configuration Release --logger "console;verbosity=minimal"
```

WPF tests require `windir` in a shell that only exposes `SystemRoot`:

```powershell
$env:windir = $env:SystemRoot
E:\Java\dotnet-sdk-10\dotnet.exe test studio\src\DarkGreyRPG.Studio.Wpf.Tests\DarkGreyRPG.Studio.Wpf.Tests.csproj --no-restore --configuration Release --logger "console;verbosity=minimal"
```

Runtime command:

```powershell
$env:JAVA_HOME = 'E:\Java\jdk1.8.0_471'
$env:GRADLE_USER_HOME = 'E:\Java\gradle'
.\gradlew.bat build --offline --no-daemon --console=plain --quiet
```

## Evidence boundary

This baseline proves compilation and automated Core/WPF/Runtime checks only. It
does not prove 0.3.0.0 data migration, a Forge Dedicated Server lifecycle, real
window UI acceptance, or the Minecraft client vertical slice.
