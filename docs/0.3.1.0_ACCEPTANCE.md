# DarkGrey_RPG 0.3.1.0 Acceptance Record

> Date: 2026-08-30
>
> Branch: `codex/0.3.1.0`
>
> Status: local 0.3.1.0 implementation and PLAN DoD are `GO`. Cases A-L,
> Stage 6 steps 1-14, Dedicated Server restart/reload, two-player isolation,
> CustomNPC+ compatibility, and cross-dimension state sharing have live evidence.
> Source publication to `codex/0.3.1.0` was authorized on 2026-08-31. This does
> not create or update a tag, PR, GitHub Release, or binary asset.

## Evidence classes

- Probe/test evidence proves a deterministic code contract.
- Dedicated Server startup proves Forge server loading and lifecycle.
- Minecraft client startup proves mod compatibility and a visible client.
- Real GUI/gameplay acceptance requires direct interaction with the displayed
  Studio/Minecraft UI. One class is never reported as another.

## Automated results

- Studio Core Release tests: `344/344` passed.
- Studio WPF Release tests: `328/328` passed, including the real Shell command
  that exports the selected Story into a server-ready package directory.
- Java 8 compile and focused Story Package, runtime, Nominator, Copier,
  Storage Box, identity, CNPC resolver, persistence, codec, and full canonical
  Story vertical-slice probes pass.

The final local candidate passed Java 8 `compileJava + testClasses + jar +
reobfJar + sourcesJar + 38/38 JavaExec` in one gate (`BUILD SUCCESSFUL in 43s`,
122 PASS markers). All 32 F/G/H-related Java files passed the Spotless IDE hook;
full-repository `spotlessCheck` remains blocked by unrelated pre-existing dirty
files and was not globally applied.

## PLAN minimum matrix mapping

- A unique NPC: identity conflict/unbind/transfer/restart probes.
- B collective Actor: stable multi-identity resolver and Task dispatch probes.
- C exact Item ID: ItemStack identity roundtrip/mismatch probes.
- D/E Item Groups: exact/fuzzy and multi-Group probes.
- F cross-Story logic: public Logic graph, Story Package merge, actual server
  propagation, player-B isolation, and ONCE non-repeat all passed.
- G Condition: both one-branch directions have durable regressions; the live
  false wait survived a server restart, then resumed through its connected true
  outlet when the upstream public Logic value changed.
- H Objective activation: night `7/10`, day ignored without reset, then night
  resumed to `10/10 SETTLED` through live world-time binding.
- I Settlement priority: first-true-only Task runtime probes.
- J multiplayer: UUID-isolated Story/Task/player SavedData probes.
- K CNPC copy: external registry and reflection-safe fallback probes plus real
  CustomNPC+ Cloner identity acceptance passed.
- L Storage Box: survival identity reservation, creative identity-free template,
  and real survival/creative interaction acceptance passed.

## Manual and live acceptance

Completed in the local post-freeze candidate:

- fresh Studio window, migration UI, and real menu Story Package export;
- fresh Dedicated Server load/reload/bad-manifest retention/restart/save/stop;
- fresh Minecraft client main menu with and without optional CustomNPC+;
- corrected Priority 5 fixture real reload and a real CustomNPC+ Developer
  client join/render on disposable worlds R2/R3;
- a dedicated `DarkGrey RPG` creative tab containing all six mod items;
- a Villager created with the vanilla spawn egg remained present instead of
  disappearing immediately after spawn;
- with the Nominator held, right-clicking that Villager opened the entity page
  with server-authoritative target `Villager · Villager`, while right-clicking
  air opened the inventory page;
- both Nominator pages were visually checked at the 427x240 logical minimum
  (854x480 physical) and at 1902x954, with labels, values and controls no longer
  overlapping;
- the first of two Villagers was bound to individual Actor `tavern_boss`, the
  second Villager's conflicting bind was rejected, and the first binding was
  re-read after a real server/client restart;
- ordinary interaction with the bound Villager opened `acceptance_offer`, then
  activated `acceptance_slime_task` at `0/10`;
- real player kills advanced the objective to `10/10`; Task SavedData records
  `SETTLED`, objective `COMPLETED`, progress `10`, and result port `done`;
- `acceptance_thanks` opened, 10 `darkgrey_rpg:copper_coin` items entered the
  player's inventory, and Continue closed the screen normally;
- final SavedData records Story `acceptance_bartender` at `TERMINATED`, no active
  Session instance, and one reward receipt. The user later deliberately threw
  away the coins, so their absence from a later player save is not a delivery
  failure;
- a real `/dgr reload` after completion preserved Story `TERMINATED`, Task
  `SETTLED 10/10`, Objective `COMPLETED`, zero active Sessions/continuations and
  one reward receipt.

No PLAN minimum-matrix case remains pending. Exact wolf-species nomination and
automatic CNPC resurrection identity migration remain optional compatibility
variants, not substitutes for the accepted unique-ID and fail-closed semantics.

F/G/H and cross-dimension evidence is under
`.tooling/stage6/gameplay-acceptance-20260830/live-case-fgh-20260830/`:
`pre-restart-latest.log.gz`, `latest.log`, `dimension-latest.log`,
`13-verifierb-connected.png`, and `15-dimension-nether.png`.

### Priority 5 fixture boundary

The reusable fixture is under
`.tooling/stage6/gameplay-acceptance-20260830/fixture-project`; the live copy is
under `live-case-a-main/runtime-fixture-project`. Its corrected project root and
canonical membership passed a real `/dgr reload`. The player-A portion of the
PLAN Stage 6 canonical test steps 1-12 and step 14 are live-proved through nomination,
Session, Task `0/10` -> `10/10`, Settlement, thanks Session, 10 copper coins and
Story termination plus a post-completion reload. A separate `VerifierB` joined
the same server and remained absent from all DGR Story, Session, Task and reward
SavedData, proving step 13 and completing the Stage 6 1-14 matrix.
See the R2/R3 reports and `live-case-a-main/ACCEPTANCE-REPORT.md`.

## Known compatibility boundary

CustomNPC+ exposes no proven stable resurrection/clone transfer key in the
inspected 1.11.1 fixed-v1 API. When resurrection creates a new UUID, automatic
unique-ID migration is not claimed; explicit Nominator transfer is the safe
supported route.

## Development-preview artifacts

- Runtime JAR: `build/libs/darkgrey_rpg-0.3.1.0.jar`, 779,979 bytes,
  SHA-256
  `D0C1273C2844DCA9DD8FBF824FC2F89EFCB0CA671E597706A9218BCA70D6259D`
- Sources JAR: `build/libs/darkgrey_rpg-0.3.1.0-sources.jar`, 347,129 bytes,
  SHA-256
  `8CDAA0DCAF2337D546B1AE9EB4747CFE7F2CDE4F2232A074E28517E543FDAFD5`
- Studio EXE: `dist/DarkGreyRPGStudio/DarkGreyRPGStudio.exe`, 141,270,742
  bytes, FileVersion/ProductVersion `0.3.1.0`, SHA-256
  `91963B5D4CF65244CEA0AC2C57DA67735F086E4C8357785856984E0FD1E6C5E7`
- source commit: the commit containing this record on `codex/0.3.1.0`
- remote branch: `codex/0.3.1.0`
- GitHub pre-release name: `0.3.1.0_开发版`
